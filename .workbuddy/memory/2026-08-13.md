# 2026-08-13 工作日志

- 【回测 CSV 补充 symbol + 排序】(承接 08-12 回测) backtest_short_top1.py: trades 加 symbol 字段(首列), 输出按 openTime 排序; 修复统计索引(t[4]->t[5] 因插入 symbol 后移)。结果不变: 177 笔 / 胜率 61.0% / 平均 -0.958%。CSV 列: symbol,openTime,openPrice,closeTime,closePrice,pnlPct,reason。
- 数据规范(定版): openTime/closeTime = UTC 毫秒 int; openTimeLocal/closeTimeLocal = 北京字符串; collection 命名大写 SYMBOL.BINANCE。96 上榜币已按此规范入库(重命名小写->大写)。
- 回测数据准备: ensure_close_map 先查库(覆盖够不拉), 不足才拉取, 单币最多重试 3 次, 仍缺标记 None 跳过该币。本次 96/96 全覆盖、零拉取。
- 【止损逻辑修正(用户指出)】原用 close 判断止损(px>=2x 开仓价), 会延迟触发且跳空巨亏(-125%)。改为: 每根 5m K 判 open/high 是否触及止损价——开盘跳空越过(open>=stop)按开盘价成交, 盘中触及(high>=stop)按止损价成交, 单笔亏损封顶 -100%。ensure_close_map 改存 {open,high,close} 三价。结果变化: 178 笔 / 胜率 60.7% / 平均 -1.333%(旧 177 笔 -0.958%), 止损 8->10 笔且全部固定 -100%(1 笔跳空 -100.4%)。策略仍负期望。
- 【🔴禁仓 bug(用户质疑"确定有在执行吗")】`ban[sym]=tick` 只存平仓时刻, 开仓检查 `ban.get(sym,0)>tick` 恒为 False(平仓时刻是过去) → **禁仓从未生效**, 且同 tick 先平后开导致间隔 0h 再开仓(akeusdt 07-15 15:40 止损后同刻开仓)。数据验证: 178 笔中 67 对同币相邻开仓违反 7 天禁仓。修复: `ban[sym]=tick+BAN_MS`(禁仓截止=平仓+7天)。修复后: **116 笔 / 胜率 65.5% / 平均 +0.614%**(转正!) / 止损 5 / 违反 0 对。⚠️ 此前所有回测结果(-0.958%/-1.333%)均基于坏禁仓, 作废。
- 【策略优化诊断+回测】(backtest_diagnose.py + backtest_short_top1.py 参数化) 诊断 116 笔: ①5笔止损全在入场涨幅30-60%区间, 涨幅≥60%静态零止损(但动态回测加 MIN_CHG 过滤反而更差, 阈值越高越差 MIN_CHG=80 只剩34笔-12% → **入场过滤证伪**, 静态分桶有幸存者偏差); ②盈利单中途最大浮盈+25.1%到期实得+18.4%回吐6.7% → 加止盈锁利; ③持仓12h+5.61%优于24h+4.32%。参数化(环境变量 MIN_CHG/TP_PCT/HOLD_H/STOP_PCT), 止盈需补查 low 字段(KeyError 教训)。**最优组合 HOLD=6h + TP=15%(回落止盈) + STOP=50%(反向止损) → 120笔/胜率63.3%/平均+2.718%/等额总盈亏+326%**(基线 +0.614%/+71.2%), 止损5笔-100%→2笔-50%封顶。核心逻辑: 短持仓减少暴露在"继续暴涨"风险时间 + 止盈锁"先跌后反弹"利润 + 紧止损封顶单笔亏损。⚠️过拟合风险: 单一样本(07-01~08-11)调参, 需样本外验证(可用 change_20260811_20260812 数据)。
- 【⚠️优化已撤回(用户指令)】backtest_short_top1.py 已还原为基线版本(去掉 MIN_CHG/TP_PCT/HOLD_H/STOP_PCT 参数化、止盈分支、涨幅过滤), 保留两处用户认可的正确修复(止损 high 触及判定 + 禁仓 tick+BAN_MS)。backtest_diagnose.py 与 backtest_diagnose.csv 已删除。基线结果回归: 116 笔 / 胜率 65.5% / 平均 +0.614%。
- 【涨跌幅阶段缺失重试改 3 次(用户指令)】binance_24h_change_all.py process_symbol: 缺失拉取从"只拉1次"改为循环最多 3 次重试(与回测 ensure_close_map 同策略), 重试后仍缺 → 不阻断, 计算阶段逐点标 error(保持涨跌幅"逐点 error"语义, 区别于回测"整币拉黑")。坑: change_all 的 local_str 接收 datetime 而非毫秒, print 需 local_str(ms_to_local(ms)) 转换。
- 【开仓确认规则(新增, 数据已备)】规则: 开仓前确认 过去一天最高价 >= 过去十五天最高价(=最近24h创15天新高) 才开仓; 数据不足15天按"上市以来"算(用户选B)。backfill_backtest_klines.py 每币 K 区间头部 首上榜-5min → **首上榜-15天-5min**(LOOKBACK_MS=15*DAY_MS)。补拉完成: 96币 0失败, 每币新增~4320根(15天×288), 95/96 新区间完整; **capusdt 例外**(06-27 上市, 历史不足15天, 属B场景)。
- 【开仓确认已实现+回测】backtest_short_top1.py: ①ensure_close_map 头部前移15天, 完整性改为按"实际数据起始(上市时刻)到end连续"判定(上市晚的币不误拉黑, 解决 capusdt 场景); ②新增 confirm_new_high()(bisect 定位窗口 + max high, 不足15天自然从上市以来算), 开仓检查接入。结果: **81 笔 / 胜率 67.9% / 平均 +0.669% / 止损 4**(基线 116笔/65.5%/+0.614%/止损5)。验证: akeusdt 07-15 13:00 跳空止损单(-100%)被确认拦截(24h高0.000281<15d高0.000519), 但 taikousdt 换时间点(08:40→17:50)仍止损, akeusdt 15:40 又开仓亏-61.6%。**结论: 规则有效但温和(+0.055pp/胜率+2.4pp/止损5→4), 非灵丹妙药。**
- 【窗口扫描 7天/30天】LOOKBACK_MS 参数化后扫描: 7天=97笔/66.0%/+0.450%/总+43.7%(劣于基线, 过滤太松放行假突破); **30天=71笔/66.2%/+0.971%/总+69.0%/止损4(最优, 需补拉头部至首上榜-30天, 96币0失败)**, 15天=81笔/67.9%/+0.669%/总+54.2%。规律: 窗口越长平均盈亏单调升(0.45→0.67→0.97%), 过滤越严(97→81→71笔), 但胜率30天(66.2%)略低于15天(67.9%)。⚠️样本递减(71笔)过拟合风险增。CSV 加 cumPnl 累计列(等额本金口径逐笔累加, 末行=总盈亏)。
- 【禁仓规则优化(用户指令)】盈利平仓 → 取消7天禁仓(ban=0, 可立即再开); 亏损平仓 → 保持7天禁仓(ban=tick+BAN_MS)。兜底段一并统一(修正原 ban=ks[-1] 少加 BAN_MS 的隐性不一致)。30天窗口结果: **76笔/胜率65.8%/+1.670%/总+126.9%/止损4**(优化前 71笔/66.2%/+0.971%/总+69.0%)——平均盈亏+72%, 总盈亏近翻倍。验证: 盈利后<7天再开 5笔(允许), 亏损后<7天再开 0笔(严格执行)。逻辑: 盈利=该币做空靠谱(会回落), 再暴涨上榜继续做空胜算高, 旧一刀切禁7天错过机会。
- 【反转指标引入+本地化】esportsusdt 5m 反转信号可视化(07-15~07-20): 引入外部 tradingview_reversal 的 reversal_score(19指标共振计数), HTML 展示 K线+▲▼信号+阈值滑块(默认3,1-8可调)+悬停显示时间/信号明细。生成脚本 plot_reversal_esports.py + 输出 data/esports_reversal_0715_0720.html(1728根K, bull>=3:294根, bear>=3:352根)。用户要求: 指标代码复制到项目内 workbuddy/reversal_indicators.py 直接 import, 不再 sys.path 依赖外部项目; 重跑结果一致。
- 【96币 reversal_score 批量可视化】plot_reversal_all.py: 每个 Top1 合约一个 HTML(esports 同款模板: K线+▲▼+阈值滑块1-8+悬停明细), 时间窗=该币首信号前24h~末信号后24h(计算前再预热48h), 3并发20秒生成96个文件共33MB, 0失败, 输出 workbuddy/data/top1_reversal_score_html/{symbol}.html。抽查 esportsusdt: 4972根K(07-08 21:20~07-26 03:35), bull>=3:735根, bear>=3:925根。
- 【规则演进(用户迭代)】①reversal开仓确认: top1信号后两根5m K内 reversal_score bear>=6 才开仓(pending机制, 命中K收盘价开仓); ②止损演进: 24h前高(太紧,47笔-30.6%)→1h×1.15(40笔+7.7%)→max(24h最高价,开仓价×1.15)(本次); ③确认窗口: 24h→1h(证伪: 1h创30天新高=追最猛接刀, 40笔胜率42.5%, 过滤掉净赚+88%的19笔)→退回24h。**最终: 24h确认+max缓冲止损 = 51笔/胜率45.1%/平均+2.478%/等额总+126.4%, 止损25笔触发幅度[15%,30%]**(低胜率高盈亏比结构, 平均盈+22.9% vs 平均亏-14.3%, 盈亏比1.6)。CONFIRM_HOURS 环境变量可调确认窗口。对比: 1h×1.15止损+24h确认=51笔/49.0%/+3.181%平均(略优); 无reversal的100%止损+盈利免禁仓版=76笔/65.8%/+1.670%/总+126.9%。
- 【系统性遗漏修复(用户发现)】旧 pending 版检查"信号后两根K(T,T+5min)", 但 bear 共振峰值往往在"信号当下K(T-5min)"(冲高见顶那根), 之后指标消散 → 系统性漏掉 132 个信号点(6046 中 2.2%, 如 esportsusdt 07-19 15:10: 当下K bear=6, 后两根 3/0)。修复: 窗口改为**当下K(T-5min)+下一根K(T)**, 任一 bear>=6 开仓。修复后 esportsusdt 07-19 15:10 开仓 0.05155→0.02096 **+59.34%**。排查脚本教训: reversal_score 需完整预热(Connors rank50 需~50根), 短窗口(14:00起13根)会把 bear 低估(2 vs 真实6)。当前 41笔/胜率4.9%/平均-0.410%/等额-16.8%: 39笔止损(1h前高裸用擦伤-2.19%均)+2笔大赚(+59.3%等)。
- 【回滚到最简版(用户:累了)】移除全部迭代复杂度: reversal开仓确认(rev_map/pending/REV_MIN_BEAR)、1%风险仓位(positionPct/accountPnl)、1h止损演进(min(2x,1h最高价)及缓冲)、1.01×开仓过滤、24h 3次窗口护栏。backtest_short_top1.py 重写为最简版: Top1信号直接开空 + 创30天新高确认(24h窗口) + 100%止损(high触及) + 盈利免禁仓/亏损禁7天 + 24h到期, CSV恢复等额口径(pnlPct+cumPnl)。回归验证: **76笔/胜率65.8%/平均+1.670%/等额+126.9%**(与8-13早上最优一致)。教训: 迭代中多次验证——前高止损裸用必擦伤、止损后复开同币=接刀、1h创新高确认=追最猛; 最简版反而是最优。
- 【周期扩展 06-01~08-11 + 模拟平均盈亏过滤(未完成, 明早续)】①06-01 起涨跌幅全量: 527币完成(ok=111828 err=44185, 11币部分error: ARX/BTW/CAP/DATAIP/DOS/GRAM/GRVT/OUSDT/REUSDT/SLX/ZEST), 输出 data/change_20260601_20260811/; ②Top1信号: 000000_top1_rise.csv 10366点/131币(06-01~06-30 新增4320点); ③回测脚本已改: 最简版规则 + sim_avg_pnl()(回放过去30天该币模拟交易, 平均盈亏>0才开仓, 无模拟数据放行) + 信号文件/周期指向06-01; ④回测进行中: 预拉35个新币K(部分已入库), 10分钟timeout被杀, CSV未生成。**明早续跑**: 前台跑 backtest_short_top1.py(10分钟/轮, K已入库会跳过, 预计2-3轮完成)。坑: ①后台任务(run_in_background)写CSV会 Permission denied(沙箱), 必须前台跑; ②change_all 的 upsert_needed 曾 local_str(int) bug(07-01任务无fetch未暴露, 已修 local_str(ms_to_local(int))); ③DONE_FILE 加环境变量支持; ④save_done 逐币保存可断点续传。
