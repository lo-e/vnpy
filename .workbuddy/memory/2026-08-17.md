# 2026-08-17 工作日志

## 定稿 AGE12（距24h最高点时间过滤）—— 用户 00:06 拍板
- 用户发现 jtousdt 2026-05-08 17:20 开仓时距 24h 最高点 15.3h，属"接刀单" → 分析全样本：距高点越久表现越差（<6h 胜率55.2%/+1397.8pp vs >18h 胜率25%/-11.6pp）。
- 实验（backtest_entry_exp.py 加 HIGH_AGE_MAX 开关，环境变量默认0→定稿默认12）：
  - AGE18：347笔/+1475.4%（+11.6pp，= 剔 >18h 4笔 -11.6pp，理论完美命中）
  - AGE15：344笔/+1467.6%（+3.8pp，= 剔 ≥15h 7笔 -3.8pp）
  - **AGE12：345笔/+1574.4%（+110.6pp）**——超额来自连锁效应：剔除后禁仓序列变化，新开 8 笔贡献 +135.3pp，被剔 14 笔损失 +24.7pp。
- **样本外验证（05-01 切两段）**：前半 base +1034.6 vs AGE12 +1046.4（+11.8pp）；后半 base +424.0 vs AGE12 +522.8（+98.8pp）→ **两段均优，非过拟合**。
- 定稿落地：HIGH_AGE_MAX=12 写入 backtest_short_top1.py（默认 12），重跑 `_bt_pump8h_final.csv` = **345笔/加权411/胜率55.7%/毛+1574.4%**，自审通过（与 _exp2_age12 345/345 重合、0 差异；sirenusdt -100% 止损正确；jtousdt/synusdt 接刀单已剔）。清理 _exp2_* 5 个中间产物，保留 _oos_* 4 个样本外证据。
- 注意：Bash 中 `B="VAR=val"` 变量展开不会被当作环境变量赋值（command not found），须用 `env $B` 或直接写全。**⚠️ 09:30 补充：`env $B ... python script` 方式本次又失效（静默无输出无文件、exit 0）——Git Bash+Windows python 组合下 env 不可靠；已改为 `export VAR=val` 方式跑通。以后跑回测统一用 export。**

## AGE3 vs base 逐笔核对（09:30 用户要求）
- 用户问"AGE3 相对无过滤过滤的 5 条"——**实际是 56 条被剔 + 29 条连锁新开**（351→324 净减 27）。被剔 56 笔合计 +144.1pp（其中盈利单多：tstusdt +34、我踏马来了 +32、uaiusdt +36、ariausdt +45.6、gusdt +23.7 等），连锁新开 29 笔合计 -116.3pp → 净 -260.4pp = AGE3(+1203.4) - base(+1463.8) 精确吻合。AGE3 之所以崩：3h 内高点单虽是"接刀"（胜率低），但盈利单吃满回落（正贡献 +144pp），且连锁换单补不回。
- 临时文件 `_cmp_base.csv` / `_cmp_age3.csv`（已跑完待删，用户验证用）。

## 锚窗口梯度（LOOKBACK_DAYS，10:28 用户新方向）
- 用户提出测"30天锚"的梯度（7天/15天），而非只测 24h 冲高窗口。`backtest_entry_exp.py` line 37 改 `LOOKBACK_DAYS` 环境变量（默认30）。
- 结果（均带 AGE12 定稿参数）：**LB30=345笔/55.7%/+1574.4%（复现✅）；LB15=426笔/51.6%/+857.3%（−717pp❌）；LB7=508笔/53.0%/+1225.9%（−348.5pp❌）**。
- 定性：缩短锚窗口→"创N天新高"门槛降低→开仓暴增（345→508），多开单=创短期新高但没创30天新高=反弹单（30天内有更高点），做空反弹=接刀，胜率跌、收益崩。
- **结论：30天锚+24h冲高窗口+AGE12 每维都是全局最优，定稿不动。** 临时文件 _exp4_lb* 待清。

## LB45/60 卡住根因（10:48 用户判断命中）
- 用户问"往上调要补数据是不是卡的原因" → **完全正确**：
  1. **库里无 2025-11 数据**：528 币 0 个有 2025-11，最早全 2025-12-01 起（数据任务起点）。45/60 天锚预拉窗口伸到 2025-11 → 触发大规模补拉。
  2. **浮点 startTime bug**：`LOOKBACK_DAYS` 是 float → `LOOKBACK_MS` 变浮点 → Binance klines `startTime=1780034700000.0` 400 Bad Request → 274 币×3 重试卡死在预拉阶段。
- 修复：`LOOKBACK_MS = int(LOOKBACK_DAYS * DAY_MS)`（backtest_entry_exp.py line 38）。**教训：任何毫秒时间戳传给 Binance API 必须 int()。**
- 已后台重启 LB45→LB60（task cDlXhT，补拉 479 币×13~28 天，预计 30-60 分钟）；下架币（1000xecusdt 等）fapi 拉不到会被 skip（真实语义：无 45/60 天数据不该开）。
- **11:58 用户终止 LB45/60 测试**（"不用拉数据了"）→ 杀进程 28860、清理 _exp4_*/_cmp_* 全部临时文件。**教训补充**：① 1000xecusdt/1000000bobo **不是下架币**，是 2025-12-31 上市的新币（库里最早 2025-12-30 23:55）；400 真因 = startTime 早于上市时间（浮点只是次要）；② "数据有多少算多少"语义代码已支持：ensure_close_map 用 eff_start（实际数据起始=上市时刻）判完整性，confirm_new_high 数据不足退化为"上市以来新高"——预拉 skip 只发生在区间中间真缺口（浮点 bug 导致），修 float 后自愈；③ 杀进程用 PowerShell `Stop-Process -Id <pid> -Force`（Git Bash taskkill 参数转义有问题）。
- **锚窗口梯度最终结论（30/15/7 已测，45/60 用户终止未测）**：LB30=+1574.4% 最优；LB15=-717pp、LB7=-348.5pp 崩塌。45/60 未测完（数据缺口大+用户终止），按趋势 30 已是最优。实验脚本 backtest_entry_exp.py 保留 LOOKBACK_DAYS 开关（int 修复后版本）。

## LB45/60 同窗口重测（12:06-12:30 用户新方案）
- 用户方案：**START_DT=2026-02-15**（信号期 2-15~8-12），60 天锚只需 2025-12-17 起数据，预期库里全覆盖不拉新。
- **发现：库里不是全量库，是按需预拉库**——每币只覆盖历史预拉过的窗口（[首上榜-30d, 末上榜+24h]），45/60 天比历史窗口多伸的 15~30 天 + 部分币整段缺失（如 onusdt 补 48907 根）→ **仍触发 fetch 补拉**（LB45 首轮 10 分钟拉 38 币、0 失败 0 skip，网络通，但慢）。
- LB30 同窗口基准已出：**273笔/加权323/胜率54.6%**（_exp5_lb30.csv，cumPnl 待拉）。LB45/LB60 后台跑（task Ln8exP，预计 30-50 分钟）。
- **⚠️ 库数据形态实证（12:38 用户追问"怎么判断没拉过"）**：库里**不是全量连续库，是历次任务按需拉取的并集，且存在大量"筛子状"历史缺口**——抽查 orcausdt：库K 48996 根、缺口段 15517 处，缺口按月份集中在 **2025-12(145)+2026-01(386)（数据任务起点 12-30 附近，12-30~01-03 段实测 433/866 缺一半）** 和 **5~8月（每月 4000+ 处）**；fetch 过的币和未 fetch 的币左边界均 ✅ 覆盖 45d 窗口起点（如 orcausdt 45d 起点 01-03、库最早 12-30）→ **"45/60 天多伸 15~30 天完全没拉过"的说法不成立**，真实机制：多伸部分藏着历史缺口 → 完整性判定 len(cm)<expect 失败 → **fetch 整个窗口**（onusdt 拉 48907 根=整窗非缺口量）。经验：改锚窗口必触发整窗重拉补缺口，一次性成本。

## 预拉逻辑重构（12:50 用户设计，重大改进）
- 用户思路（三步）：① `probe_listing` 用 startTime=2020-01-01 + limit=10 探测**上市首根K时间**（重试3次）② 按上市时间算所需区间，对比库内已有，**分头部/尾部两段补拉**（各重试3次）③ 校验根数，不完整才**全量兜底**。
- 已在 backtest_entry_exp.py 实现：新增 `probe_listing()`（PROBE_EPOCH=2020-01-01）+ `build_cm()` 辅助；`ensure_close_map` 重写为 probe→分首尾补→校验→全量兜底。**第一版有 bug**（head_gap 置 None 后条件永不满足→全部掉全量兜底），已修（head_gap/tail_gap 布尔标记）。
- **实测效果（小窗口 2-15~3-15 验证）**：38 次"补头部"、每次恰 4320 根（=15 天缺口 = 45d−30d 差额）、0 全量兜底、0 skip、回测正常（34笔）——**对比旧逻辑整窗重拉（onusdt 48907 根）省 90%+ 拉取量**。
- 完整 LB45/LB60（2-15~8-12）已用新逻辑后台重启（task LzKrOE）。

## 数据任务规则变更（15:19 用户拍板）
- **取消"缺失 ≤5 根忽略"容忍**（binance_24h_change_all.py 两处）：① missing_left ≤5 清零删除；② 整币状态 `err<=5→ok` 改为 `err==0→ok`。动机：分段拉取已实现（按缺失点分组、只拉缺失段），"小缺失触发全盘重拉"的旧顾虑不存在，≤5 缺失应照实标 error。
- 联动：err>0 → done=error → 下次任务 todo 重跑该币（自愈机制，重跑再拉一次网络好则补上变 ok）；永久缺失币（下架/上市晚边界）每次任务重跑但成本低。
- 历史数据 change_20260101_20260812 不受影响；done.json 已 ok 币不会自动重校验（要全量重校验需清 done.json）。

## 完整回测 01-01~08-15（15:33-16:00 用户拍板 A 开干）
- 用户要求完整回测窗口延伸至 **08-15**。执行：
  1. **同步新预拉逻辑到定稿脚本**（backtest_short_top1.py：PROBE_EPOCH/probe_listing/build_cm/新 ensure_close_map，与实验脚本 diff 逐字一致）+ **SIGNAL_FILE 参数化**（环境变量覆盖）。
  2. 数据任务 binance_24h_change_all.py 跑 01-01~08-15 → `change_20260101_20260815/`（32545 点/527 币，ok 1654 万行 err 60.9 万行=3.5%，失败币 50 个按新规则如实标记）；top1_analysis.py 重生成信号文件（32545 行到 08-15 00:00，首 3 与旧窗口一致=前段数据未变）。
  3. 回测：**353笔/加权420/胜率55.5%/毛+1592.8%**（`_bt_pump8h_final.csv`）。
- **自审通过**：与旧定稿(345笔)前段 345/345 重合、pnl/reason/closeTime 0 差异；新增 8 笔(08-12~15) 4赚4亏净+18.4pp；sirenusdt -100% 止损正确、weight 全 1/2、pnl 范围正常、11 列含 maxAdvPct。
- 曲线 `_curve_pump8h.html`（353 笔/+1592.78%）已生成。旧定稿 345 笔保留在 change_20260101_20260812/ 作对照。
- 过程日志：workbuddy/_change_20260101_20260815.log、_bt_20260101_20260815.log。

## 🔥 半入库根因发现与修复（16:34 用户关键发现）
- **用户发现**：数据任务 `binance_24h_change_all.py` 的 `upsert_needed` 有过滤 `if ot_ms not in needed_ms: continue`——**只入库每 10 分钟 1 根**（needed_ms = {T-5min}∪{T-24h-5min}，每10分钟1个时间戳），虽然 fetch 拉的是全量段。
- **这就是之前所有"筛子状缺口"的真正根源**：orcausdt 缺口段 15517（=每10分钟缺1根）、12-30~01-03 段 433/866 缺一半、akeusdt 补尾后仍不全（中段缺非计算点 K）——回测每次为"半入库"补拉。
- **修复**：`upsert_needed` 去掉 needed_ms 过滤 → 拉到的全量 5m K 全入库（只去重）。语法 OK。
- **存量处理**：01-01~08-15 回测的 ensure_close_map（校验完整窗口+补拉）实际已把参与币窗口内数据补齐为全量（akeusdt 61275=期望 61275 证据）；未参与币下次遇到时补。以后数据任务新拉段直接全量入库，回测补拉量大减。

## 币种清单自动校验增强（17:07 用户要求）
- `binance_24h_change_all.py` 新增 `auto_sync_symbols()`（json 读取后、任务前自动调用）：exchangeInfo 拉当前 TRADING 的 USDT 永续 → **新币加入 json 并落盘（只增不减）、下架币保留不删**（防历史窗口重跑漏币）；拉取失败警告沿用现清单不阻断。
- 实证：exchangeInfo 当前 527 = json 527，零差异。json 结构 = 大写 symbol list（含中文符号币）。

## 完整回测 12-01~08-16（17:10-19:40 用户要求）
- 窗口 **2025-12-01 ~ 2026-08-16**：数据任务 37153 点/527 币（ok 1872 万行 err 85.7 万行=4.4%，失败币 60 = 上版 50 + **10 个 12 月上市新币**（BREV/CYS/GUA/LIT/NIGHT/POWER/RAVE/US/WET/ZKP，上市前点缺 24h 前价逐点 error，预期合理）；top1 信号 37153 行；回测 **399笔/加权478/胜率56.4%/毛+1697.1%**。
- **自审通过**：与上版定稿(353笔)前段 353/353 重合、0 差异；新增 45 笔 12 月信号净 +104.3pp；stop_loss 2 笔（xnyusdt 12-05 + sirenusdt 02-08，均-100% 翻倍止损正确）；293/293 币参与、0 缺失（数据完整性提示 ✅）；weight 全 1/2、pnl [-100,74.1]。
- **注意**：12-01 窗口的 30 天锚需 11 月完整 K（库里没有）→ 回测预拉补头部（47 行 fill）→ 前台 10min 超时被杀 → 改后台跑成功；11 月数据已全量入库，后续快。
- 曲线 `_curve_pump8h.html`（399 笔/+1697.14%）已生成。日志：change_20251201_20260816/_change_*.log、_bt_*.log（已按窗口目录放）。

## 固定止盈 TP80 定稿（20:21-21:00 用户拍板）
- 用户思路："暴跌80%几乎24h极限，设止盈80%落袋"。实验脚本加 `TAKE_PROFIT_PCT`（默认0=关），两分支（PUMP_ENABLE else / else）在止损前加固定止盈（盘中 low 触及按止盈价）。
- **梯度**（12-01~08-16，399笔基准）：TP80=+1731.5%(+34.4pp✅,1笔)/TP70=+1722.1%(+25pp✅,3笔)/TP60=+1682.1%(−15pp❌)/TP50=+1646.4%(−50.7pp❌)。**双刃剑**：防回吐单赚（ariausdt 跌80%反弹，base+45.62→TP80锁+80，+34.4pp）、能涨单亏（arcusdt base+74.1 被60档锁在+60）。**8.5月仅1笔到+80%**=尾部保险性质。
- **定稿落地**：backtest_short_top1.py 加 TAKE_PROFIT_PCT（**默认80**）+ 两处止盈逻辑，重跑 final = **399笔/478/56.4%/+1731.5%**，自审通过（与 _exp_tp80 逐笔一致；take_profit 1 笔 ariausdt；stop_loss 2 笔；weight/pnl 正常）。已清理 _exp_tp_* 5 个；曲线更新（+1731.5%）。
- ⚠️ 中途发现并修复：**实验脚本 SIGNAL_FILE 未参数化**（写死旧路径导致 base 复现成 345 笔旧窗口）→ 已改 os.environ.get，与定稿一致。

## 止损 100%→80% 评估（21:05-21:45 用户要求）
- **结果**：STOP_FACTOR=1.8+TP80 = **313笔/57.2%/+1690.0%（−41.5pp❌）**。负优化。
- 拆解：① 相同开仓点 313 笔仅 2 笔变化（sirenusdt −100%→−80% 省20pp、evaausdt −84.5%→−80% 省4.5pp，合计+24.5pp）；② **86 笔开仓被连锁禁仓挡掉**（止损更近→亏损禁仓→后续信号被禁），这 86 笔在 100% 口径合计 **+66.0pp 正贡献**（46赚40亏）→ 净 −41.5pp。
- 86 笔中关键：xnyusdt 12-05 −100% w2（−200pp 加权大亏，被挡掉是好事）但其他 85 笔合计 +266pp；大盈利 theusdt+55/w2、triausdt+41.9、我踏马来了+40.9、币安人生+35.9。
- **结论：止损 80% 负优化，定稿维持 STOP_FACTOR=2（100% 止损）**。样本外分段因 Mongo 挂没跑完。
- ⚠️ **21:54 重大纠错：止损 80% 是正优化，之前"负优化/86笔被挡"结论作废！** 旧 _exp_sf18.csv（313 笔）是**数据污染产物**：21:08 跑时 Mongo 不稳定，部分币（龙虾usdt 等）预拉失败进 missing_syms → 整币跳过 → 假"连锁消失 86 笔"。重跑（数据完整版 _exp_sf18_v2.csv）：
  - **止损 80%（STOP_FACTOR=1.8+TP80）= 399笔/56.4%/**+1796.0%**（相对 100% 定稿 +1731.5% 提升 +64.5pp ✅）**
  - 开仓点 399/399 全重合，仅 3 笔 pnl 变化：xnyusdt 12-05 −100→−80（w2，省40pp）、sirenusdt 02-08 −100→−80（省20pp）、evaausdt 07-07 −84.5→−80（省4.5pp）
  - 龙虾usdt 08-10 17:20 定稿里是 **expire +8.73%（非止损）**，被污染版误判为"被挡"
- **教训**：回测结果出现大规模"连锁消失/笔数骤减"时，必须先查 missing_syms（数据完整性提示行）再下结论——这次差点把正优化误判成负优化。旧 _exp_sf18.csv 已删，保留 _exp_sf18_v2.csv。

## 止损 80% 定稿（22:13-22:41 用户拍板）
- **样本外验证通过**：前后段×两种止损 4 variant（Mongo 同命令启动跑通；循环 set -- 拆空格时间出过错，改独立命令）：前半 100%=+869.6% vs 80%=+929.6%（+60pp✅，xnyusdt w2 省40+sirenusdt 省20）；后半 100%=+758.3% vs 80%=+762.8%（+4.5pp✅，evaausdt 省4.5）→ **两段均优，非过拟合**。
- **定稿落地**：backtest_short_top1.py `STOP_FACTOR` 默认 2.0→**1.8**，重跑 final = **399笔/478/56.4%/+1796.0%**（相对上版 +1731.5% 提升 +64.5pp）。自审通过（与 _exp_sf18_v2 逐笔一致；stop_loss 3笔均-80%：xnyusdt/sirenusdt/evaausdt；take_profit 1笔 ariausdt+80%；pnl[-80,80] 对称封顶）。清理 _exp_sf18_v2 + _oos_* 4个+4log；曲线更新（+1796.0%）。
- **最终策略全貌**：AGE12 接刀过滤 + TP80 固定止盈 + SF18 止损80%，399笔/56.4%/+1796.0%。
- ⚠️ **MongoDB 21:0x 起连接失败**（WinError 10061）：后台启动 mongod 全部失败（bash & / nohup / cmd start / run_in_background / PowerShell 均被环境清理或路径问题），**只有前台能跑**。根因含：双反斜杠路径 `C:\\Program Files\\...` 传参错误（须单斜杠 `C:/...`）；**同一命令内 `mongod & + sleep + 回测` 才能跑通**。教训：以后回测前先确认 Mongo 活着，mongod 启动用单斜杠路径。

## 收尾（00:15 用户追加要求）
- 按用户要求删除：`_oos_*` 4 个样本外数据 + 旧版 `_bt_pump8h.csv/.log`（上一版定稿）。回测相关仅剩 `_bt_pump8h_final.csv`。
- **新工作流规则（用户拍板）**：每次定稿必须更新盈亏曲线 —— `plot_final_curve.py`（CURVE_SRC/CURVE_OUT 参数化）生成 `_curve_pump8h.html`。本次已生成：345 笔 / +1574.38%。

## confirm_new_high 优化方向全部证伪（00:30-09:05）
- 用户提出优化"24h冲30天新高"确认 → 系统验证 5 个方向，**全部无肉/有害，confirm_new_high 维持 24h 窗口**：
  - B 距30天新高时间：93% 单在 6h 内，AGE12 已覆盖，无肉
  - A close确认新高(防插针)：19 笔"插针单"胜率 57.9%、合计 +190.5pp 是正贡献，过滤反而损失（盘中插针创新高=币仍强势上攻）
  - C 新高超越幅度：数学上恒为 0（confirm_new_high 强制 24h最高==30天最高），无区分度
  - D 30天新高次数：96% 单是"反复创新高"(4+次)，首次新高样本仅 5 笔，无证据
  - **AGE12h 以下梯度（AGE6/3/1）**：全线崩塌（-91.5/-260/-371/-171pp），**AGE12 确为全局峰值(+110.6pp)**
- **关键等价关系（重要）**：`AGE N ≡ NH N`（数学等价）——confirm_new_high 强制 24h最高==30天最高，所以"24h 高点在 N 小时内" ⟺ "N 小时内创 30 天新高"。AGE 梯度测试与 NH 梯度测试是同一个实验。
- 已清理 _exp_age6/3/1、_exp3_* 8 个实验文件。
