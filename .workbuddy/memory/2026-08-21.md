# 2026-08-21

## 01:40 信号脚本三改进落地(超三条指令) —— run10 干净运行 ok/err=9/0
- **改进1(probe上市时间)**: binance_24h_change_all.py 加 probe_listing(startTime=2020 limit=1 重试3次) → need_start_ms = max(START_MS, listing_ms) → 上市前点标"上市前无数据"不计err; 上市不足24h点(T-24h在上市前)标"上市不足24h"也不计err → 上市后可用点完整即 ok。
- **改进2(Retry-After动态等待)**: fetch 异常读 e.response.headers[Retry-After] 动态 sleep(上限60s), 替代固定5s。
- **改进3(代理自愈)**: fail_streak 全局计数, 连续5次失败 → check_proxy(fapi time 8s) → 不通循环等60s直到恢复。
- **踩坑记录**: ①run6/7 残留进程与 run8 并发写 done.json 污染判定(多进程必须全杀) ②need_start 提前5min会假阳性缺1根(上市首根前无K) ③上市后24h内144点"真缺失"实为边界(24h lookback需上市前数据), 已归类不计err。
- **run10 状态**: 523币待处理, ~27s/币 ≈ 4小时, probe正常(1000CAT上市2024-10-21所需58341点/1000FLOKI老币100945点), ok=9/err=0, 无429。后台挂跑(done.json续传+_change_run10.log)。
- **待办**: 完成后 → 回测(SIGNAL_FILE=change_20240101_20251201) → 月度regime对比(2024牛市/2025震荡/2026熊市)。

## 08:00 纠错(超指出): 代理挂时未中断等待的根因 + 修复
- **超问**: 之前代理挂了你没有中断等待吗? 现在一堆 error。
- **根因(双重)**: ①`probe_listing` 失败**未接入代理自愈**(wait_rate_or_proxy 只覆盖 fetch 路径) → 代理挂时 probe 3次失败直接标 error(run10: probe失败156次) ②run10 进程跨轮次被环境回收(245币后死), done.json 保留 206 error。
- **修复**: process_symbol 的 probe 失败 → `check_proxy()`: 不通则循环等60s直到恢复再重试 probe(不再直接标error)。
- **run12(修复后, 代理已恢复)**: 488币待处理(39 ok跳过+206 error重算), 30/488 完成, **probe失败=0/代理不可用=0/429=0/skip=0, 点累计 err=0** → 健康。预计 ~4小时。
- **done.json 的 176 error 是 run10/11 残留标记**, run12 的 todo 按 `done.get(s)!="ok"` 筛选会重算它们。
- 教训: 任何网络失败路径(probe/fetch)都要接入统一代理自愈; 跨轮次长任务进程会被回收, done.json 续传是唯一保险。

## 09:50 拉取效率改进落地(超四条指令) + run13 三轮机制启动
- **改进**: ①补首尾(现有missing缺口逻辑, 显式化) ②补首尾3次仍不全 → 全段兜底 fetch_all(need_start, END_MS) ③仍缺 → 整币error, 下一轮重试 ④**主循环改3轮**: 每轮处理 done!=ok 的币, 三轮后仍有缺失 → 打印缺失清单+暂停等待用户确认(不自动重试)。
- **run13 状态**: 第1轮 442币处理中(85 ok保留+357 error/未处理), 新处理币全 ok(probe正常, BAT/BCH/BEAMX 老币全量100945点), 进程存活。预计第1轮 ~4小时。
- **附带修复**: probe 失败接入代理自愈(循环等60s直到恢复)——此前漏掉导致代理挂时156币被标error。
- 待办: 三轮跑完 → 若全部ok → 回测(change_20240101_20251201) → 月度regime对比。


## 10:20 Permission denied 批量事件修复(超报告: 拉取畅通但写CSV全被拒)
- **现象**: run13 从 C 段起连续 Permission denied(12→D段全拒), 拉取正常(probe/fetch 都在出)但写 CSV 全失败 → 整币标 error。
- **根因**: 被拒文件全是 00:56~01:01 旧 run 产物(残留进程句柄/Defender 瞬时锁), run13 覆盖时被锁; 进程死后锁释放(实测全可写)。run13 进程也死于环境回收。
- **修复**: ①binance_24h_change_all.py 写 CSV 加重试(3次×3s, PermissionError 重试) ②21个非ok币旧CSV移入 _trash_oldcsv/(避免覆盖冲突) ③清21 error ④重启 run14。
- **run14 状态**: 161/161 全 ok, error=0, write重试0次(旧文件已移走无冲突), 进程存活, 待处理 366 ≈ 3小时。
- 教训: 覆盖旧文件遇 Permission denied 先查锁源(残留进程/Defender), 进程死=锁自动释放; 重跑前把非ok币旧CSV移走比依赖重试更干净。


## 11:30 根因级修复: Permission denied 批量 + 内存堆积卡顿(超报告)
- **真根因(两层)**:
  ①**内存爆炸**: process_symbol 里 `kc.distinct("openTime")` **无过滤全表**拉取(3处) + `$in: list(needed_ms)` 十万项查询 → 老币(2021-2022上市) Mongo 库 40万+根 K 线, 3并发每次拉全量 → 内存堆积几GB → 系统卡顿 → 文件系统慢/锁 → 覆盖写 CSV 被拒(Permission denied)。症状链: 老币段(C/I/D段)连败, 进程死, 卡顿。
  ②旧CSV覆盖冲突: run13/14 覆盖 00:56~01:01 旧run产物被锁(残留句柄/Defender瞬时锁), 3次重试仍败 → 整币error。
- **修复(代码)**: ①distinct/find 全加 [need_start-5m, END] 区间过滤(内存减半+, 老币从40万降到10万) ②kc_docs 用 range 查询替代 $in ③写 CSV: 先写 .tmp 再 os.replace 原子替换 + 3次×3s重试 ④函数尾 del 大对象显式释放。
- **修复(环境)**: 目录里**所有非ok币CSV**(324个, 含run14死前半成品)移入 _trash_oldcsv/ → run15 全新写无冲突; 清54 error → 186 ok保留。
- **run15 状态**: 221/221 全 ok, error=0, write重试0, 异常0, HBAR(2021老币)100945点全量正常 → 内存修复实锤。待处理306 ≈ 2.5小时。
- 教训: ①全表 distinct/超大 $in 是 Mongo 内存炸弹, 长任务必须加区间过滤 ②Permission denied 追到根因是系统资源(内存/卡顿)而非文件本身 ③卡顿先查进程内存再查锁。


## 16:45 run15 完成 → 信号合成 → 2024 回测启动
- **run15 完成**: 527/527 全 ok, error=0, 一轮搞定(无需第2轮)。日志: "全部币数据 ok"。
- **⚠️踩坑(11:20清理bug遗留)**: 主目录只有 341 csv, 缺 186 个 ok 币文件 —— 11:20 清理脚本 `splitext(base)[0].upper()+"USDT"` 多拼 USDT, 所有 ok 币被误判非ok移入 trash。**教训: 文件名已是 {sym.lower()}.csv, 直接 splitext 对比, 绝不 +USDT 再拼**。
- **恢复**: trash 验证 186 币真缺失 0%(ok 55%/边界45%), 全部移回 → 527 全覆盖 ✅。
- **信号合成**: top1_analysis.py (绝对路径 SRC_DIR) → 100800 点/23个月, 000000_top1_rise/fall.csv。⚠️前台跑 2分钟被120s超时杀(空输出exit1), 必须后台跑。
- **2024 回测**: SIGNAL_FILE=change_20240101_20251201 + START/END env, 定稿参数原样(裸跑默认MP9) → _bt_2024window.csv, 运行中(100800点/419币, 补2023底头部K线)。
- 待办: 回测完成 → 月度regime对比(2024牛市/2025震荡/2026熊市)。


## 18:10 深挖修复: 回测慢/卡爆根因 + Mongo 故障(超三条质疑)
- **超质疑**: ①回测慢卡爆(内存) ②信号为何只到11-30(要求12-01) ③日志只有补头部,尾部是否没判断。
- **问题3答案**: ensure_close_map 有 tail_gap 判断(306行 end_ms=last_sig+48h), 多数币最后上榜远早于窗口末 → 数据已覆盖不需补 → 只补头部是正确行为 ✅。
- **问题2答案**: binance END_DT=2025-12-01 00:00 排他 → 最后点 11-30 23:50。补 12-01: 写 append_dec1.py 增量追加(467币×144点全ok) → 重合成信号 100944 点(END 12-01 23:50)。
- **Mongo 故障(大坑)**: mongod 7.0.14 服务运行 ~2h 后**所有条件操作符失效**($gte/$in/$exists/$ne 全0, 仅 eq/all 正常) → 回测/append 误判数据全缺 → 疯狂补拉 → 慢+卡。修复: **重启 MongoDB Windows 服务** → 全部恢复(288根✅)。db.command 路径一直正常是诊断关键。
- **回测内存根因**: 521行预拉阶段 `ThreadPoolExecutor 3并发 _ensure 全部419币` → build_cm 全量驻留 close_maps → **15GB+** (6h释放机制在主循环内, 预拉阶段救不了)。修复: _ensure 加载后立即 pop 释放(只补数据不驻留) → **内存 2.9GB→227MB**。
- **回测 v3 状态**: 运行中(内存227MB稳定), 预拉419币(probe网络为主) + 主循环100944点 ≈ 20分钟。
- 教训: ①长驻服务(mongod)会状态腐化, 条件查询全0先试重启服务 ②预拉/预加载阶段是内存黑洞, 只补数据不驻留 ③stdout 块缓冲导致日志延迟, 用 wc -c 看真实产出。


## 18:30 两条纠错(超指出): ①回测缺代理自愈 ②信号终点语义错误
- **问题1**: 回测脚本(backtest_short_top1.py)的 fetch_all/probe_listing **没有代理自愈**(只有信号脚本 binance_24h_change_all.py 有 wait_rate_or_proxy) → 代理挂时回测不等待, 币被标缺失。**修复**: 给回测加 check_proxy()(fapi time 8s) + fetch/probe 连续失败→查代理→挂了循环等60s恢复再重试。
- **问题2(语义错误, 超批评"治标不治本")**: 信号截止语义 = **含 END_DT 时刻点**(铁证: 定稿窗口信号最后点 2026-08-16 00:00:00)。我这次合成 2024 窗口信号用 END_DT=11-30 23:50 → 丢 12-01 00:00 终点点; 且错误地 append 了 12-01 全天(00:10~23:50, 3718行) —— **正确做法: 信号合成 END_DT=2025-12-01 00:00(含终点点), 不要 append 全天**。
- **修复**: ①rollback_dec1.py 删 3718 行 12-01 00:10~23:50 点(保留 00:00) ②重合成信号 END_DT=2025-12-01 00:00 → 信号最后点 **2025-12-01 00:00:00** ✅(与定稿窗口语义一致) ③重跑回测 v4(代理自愈+正确窗口)。
- **教训**: 信号截止 = 含终点时刻点, 与定稿窗口对齐; 窗口扩展应"拉取 END_DT 含终点", 而非事后 append 补全天。
- 回测 v4 状态: 运行中(内存328MB稳定, 预拉419币+主循环100944点 ≈ 20分钟)。


## 19:15 开仓0根因: 多个 mongod 并存(超报告回测 0 开仓 + ZeroDivisionError)
- **现象**: 回测 v4 跑完但 0 开仓(主循环全程 持仓0开仓0) → ZeroDivisionError。信号/K线分布正常(2024窗口 top1 中位30.6%, 90%信号<80%可通过妖币过滤; 预拉 419/419 缺失0)。
- **根因**: **5 个 mongod 进程并存**(多次 Restart-Service 每次启新实例但旧的没退, PID 5064 一直复用造成假象) → 9GB+ 内存卡爆 + 多实例争抢同一 dbPath/WiredTiger 锁 → **条件查询周期性失效**(范围查询全0, 仅eq/all正常) → 回测主循环 build_cm 全空 → 开仓条件永不满足 → 0 开仓。
- **修复**: Stop-Process 杀全部 mongod → Start-Service 干净启动 1 个 → 查询验证 288 正常。**教训: 重启服务必须确认旧进程死透(数进程数), PID 复用会骗人**。
- **回测 v5**: 重跑中(单mongod+代理自愈+正确窗口 100801点)。mongod 单实例 5.7GB 是正常 WiredTiger 缓存。
- 待办: v5 完成 → 月度 regime 对比(2024牛市/2025震荡/2026熊市)。


## 19:35 0开仓真根因: 我的内存修复(_ensure释放)破坏 RATIO6 检查(超两轮0开仓后挖出)
- **现象**: v5/v6 单mongod+Mongo正常+预拉419/419, 仍 0 开仓。
- **调试**: 独立脚本跑前12信号过滤链 → **8/12 能通过**(含CVD/新高) → 过滤链本身没问题 → 必是回测内部差异。
- **真根因**: 我 18:10 的内存修复把 `_ensure` 改成"加载后立即 pop 释放 close_maps" → 预拉后 close_maps 全空 → 主循环 RATIO6 块(807行) `_cm = close_maps.get(sym, {})` **在 ensure(834行)之前** → _px=None → 811行 continue → **所有信号被挡**。定稿窗口正常是因为当时 _ensure 保留缓存。
- **修复**: RATIO6 块改用 `ensure_close_map(sym)`(先加载再取), None 则 continue。HIGH_AGE 空缓存是"跳过"非"挡"无需改。
- **教训**: 改内存策略(释放缓存)必须检查所有依赖 close_maps 的路径顺序(RATIO6 在 ensure 前取缓存); "能通过调试过滤链"≠"回测会开仓", 回测内部状态机时序是另一层。
- **回测 v7**: 重跑中(修复版)。debug_2024_opens.py 留作诊断工具。


## 23:10 架构重构完成: 按tick推进 → 按币推进(超指定, 解决缓存大/速度慢/中断成本高)
- **新脚本**: backtest_short_top1_persym.py(原 backtest_short_top1.py 不动, 定稿基线安全)。
- **原理**: 每个币信号互不影响(ban/permits/holdings 均 per-币, 信号每时刻唯一Top1) → 按币独立回测等价。
- **实现**: ①按币分组信号 ②每币: ensure K线(单币缓存) → 时间轴[首信号,末信号+48h]逐tick(平仓/信号/permit状态机) → trades追加 ③progress json 记录, 中断后从未完成币重测 ④处理完释放单币缓存。
- **踩坑修复**: ①import json 缺失 ②os.remove 被沙箱 safe-delete 拦截(用全新文件名绕过) ③**加仓 _in_hold 条件不满足误用 _in_hold=False 继续走(应跳过) → 重复加仓(3399 vs 1145)** ④**permit状态机内 continue 在 while tick 循环内会跳过 tick+=STEP5 → 死循环(改用 _skip_open 标志)**。
- **✅ sanity 验证(定稿窗口)**: 1145笔 逐笔0差异(同开仓点100%/改判0/总pnl +822.7%一致)。内存 211MB(vs 旧版6.5GB, 缩小35倍), 定稿窗口 7分钟。
- **2024 窗口**: 按币版跑中(419币, 内存203MB, 预计~20分钟), progress=change_20240101_20251201/_bt_2024_progress.json(可断点续跑)。
- 待办: 2024完成 → 月度regime对比(2024熊→牛/2025震荡/2026熊)。


## 23:55 2024窗口回测完成 + regime分析(按币引擎首战)
- **按币引擎跑2024窗口**: 419币/2644笔, 内存~200MB, ~12分钟(含一次Permission denied写CSV重跑, 原因: CSV被WPS/Excel占用 → 换_v2文件名)。
- **2024窗口成绩**: 2644笔/胜率44.0%/加权总pnl **+103.0%**/maxDD 189.7/**ddRatio 1.842**(差) vs 定稿2026熊市窗口 1145笔/+822.7%/ddRatio 0.068(优)。
- **月度regime分析**: BTC月涨跌 vs 策略月pnl **相关系数 -0.056(几乎无关)** → 策略不是简单BTC beta。亏最多的月: 2025-05(-63.8%)/2025-08(-66.8%)/2025-10(-54.3%)/2024-02(-61.9%); 赚最多: 2024-11(+91.4%)/2025-11(+84%)/2025-06(+67.7%)/2025-01(+55.4%)。2025-11(BTC-17%急跌)赚84%, 2024-02(BTC+44.9%急涨)亏62% —— 急涨月亏/急跌月赚的局部模式, 但整体相关性弱。
- **结论**: 收益质量牛市窗口显著恶化(1.84 vs 0.068), 但非BTC方向beta, 更可能是"妖币泡沫破裂速度"的市场结构依赖。
- 产物: _bt_2024window_v2.csv(成绩), _curve_2024window.html(曲线), _regime_compare.html(月度双窗口对比)。
- 修复: persym脚本汇总段 trades→trades_all(ZeroDivisionError); os.remove沙箱拦截改用全新文件名; progress2.json断点续传验证通过。
