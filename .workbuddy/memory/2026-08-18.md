# 2026-08-18

- 超明确权限边界：本会话/本项目只做**只读咨询**，解答 workbuddy/ 相关疑问，不擅自改动项目内任何文件；任何写操作先问。已记入 MEMORY.md 铁律。

## 回测新增 maxDD 列 + MIN30/60 综合评估（09:31 用户要求）
- 两脚本（backtest_entry_exp.py / backtest_short_top1.py）CSV 写入新增 **maxDD 列**（cumPnl 序列 peak-to-trough 最大回撤，pp 单位，每行累计值），列序 cumPnl→maxDD→maxAdvPct。
- **离线补列**（非重跑）：09:31 时 **Binance API 对代理出口 IP 返回 451 地域风控**（"Service unavailable from a restricted location"，curl 直测也 451）→ 无法重跑回测 → 对已有 CSV（_exp_nh_base/30/60、_bt_pump8h_final）离线加 maxDD 列（数值与重跑等价）。⚠️ **网络风险：代理 IP 被币安限制，后续跑数据任务/回测需换节点或等恢复**。
- **MIN30/60 综合评估（收益+回撤）**：
  | variant | 笔数 | 毛收益 | maxDD |
  |---|---|---|---|
  | base(定稿) | 399 | +1796.0% | **220.7pp** |
  | MIN30 | 313 | +1265.3%(−530.7pp) | 294.2pp(+73.5) |
  | MIN60 | 282 | +1127.9%(−668.1pp) | 225.4pp(+4.7) |
- **结论：MIN30/60 双重否定**——不仅收益大幅下降，回撤也毫无改善（MIN30 反而 +73.5pp 更差）。初始拉升期过滤彻底否定（与 08-17 结论一致：刚创新高单是 alpha）。
- 定稿 final 已补 maxDD=220.7pp（12 列）。

## 入场点 ABCEF 全验证（10:13-10:30 用户要求，新基准 AGE12+TP80+SF18 下）
- **只读约束澄清**：用户明确"只读约束只对当时下指令那个会话窗口有效"，本窗口按正常模式自行判断动手。
- **A r6 分桶质量**（定稿399笔离线算）：r6∈[0,0.5)=182笔/48.4%胜率/均值+0.86%（质量最差）；[0.5,0.85)=61.6%；[0.85,1.0)=70.5%最佳(w2放大)；≥1.0=60.0%。→ 方向：RATIO6_MIN 从-1提至0.5。
- **全部结果（综合收益+回撤）**：
  | variant | 笔数 | 胜率 | 毛cumPnl | maxDD | 收益Δ |
  |---|---|---|---|---|---|
  | base(定稿) | 399 | 56.4% | +1796.0% | 220.7pp | — |
  | A RATIO6=0.5 | 269 | 59.9% | +1352.4% | 239.2pp | −443.6pp ❌ |
  | B 停滞顶1h | 282 | 55.7% | +1127.9% | 225.4pp | −668.1pp ❌ |
  | B 停滞顶2h | 233 | 54.1% | +815.0% | 166.1pp | −981.1pp ❌ |
  | C 回撤10% | 159 | 60.4% | +713.9% | 221.9pp | −1082.1pp ❌ |
  | C 回撤20% | 19 | 57.9% | −13.9% | 144.6pp | −1810.0pp ❌ |
- **ABC 全部负优化**：A 虽胜率大涨但低r6桶整体正贡献+连锁致收益降且回撤没改善；B/C 挡掉的是刚创新高/拉升中 alpha（与 MIN 证伪同因）。**入场点无肉，定稿维持现状**。
- **E/F（资金费率/OI 背离）**：需拉 fapi fundingRate/openInterestHist 新数据，**代理 451 地域风控阻断**，暂不可行（网络恢复后再说）。
- 实验代码保留（STAGNATE_MIN/PULLBACK_MIN 参数默认关），实验文件已清理。新增参数：STAGNATE_MIN（距24h最高点≥N小时才开）、PULLBACK_MIN（回撤≥X%才开）。

## 架构级重构: 信号→开仓允许状态(permit) + ABCEF 重测（12:15-13:00 用户设计）
- **新架构(用户设计)**：信号=开仓允许状态(最长24h)，期内脱离现有规则独立运行、最多开3次(平仓后可再开,不受禁仓限制)，期内总盈亏=本次信号最终盈亏(间接禁仓: 总亏禁7天/总盈免禁)；实际开仓点前24h最高价=止损锚(与STOP_FACTOR并行,先到原则)。**寻求最佳实际入场点**。
- **实现**(backtest_entry_exp.py, PERMIT=1 开关, 默认0保旧逻辑)：permits dict + 信号→permit + permit处理循环(到期结算禁仓/期内开仓≤3次) + 平仓不禁仓改累计permit总盈亏 + 止损 min(open_px×SF, entry_h24)。ENTER_SCHEME: 0立即/1延迟N根/2回撤X%/4funding/5OI/6组合。修复兜底平仓 hold_max_high None bug。
- **测试结果(全窗, 旧逻辑=399笔/+1796.0%/DD220.7)**：
  | 方案 | 笔数 | 胜率 | 毛cumPnl | maxDD |
  |---|---|---|---|---|
  | permit立即 | 834 | 53.1% | +1589.9% | 519.4pp |
  | permit延迟30m | 592 | 52.2% | +1128.2% | 535.6pp |
  | permit延迟1h | 584 | 52.6% | +1022.9% | 560.3pp |
  | permit回撤10% | 297 | 56.2% | +209.3% | 528.3pp |
- **permit 框架性问题**：回撤翻倍(519~560 vs 220)——持仓堆积(118个)+禁仓延迟+重复开仓稀释(均盈亏1.5% vs 3.7%)；expire_forced 118笔(尾部持仓强制平仓)。立即/延迟/回撤方案全不如旧逻辑。
- **EF 数据拉取**：fetch_ef_data.py 拉 fundingRate+OI(1h) 到 ef_{sym}.csv（527币后台跑，代理已恢复451解除）；scheme 4/5/6 代码就绪(FUNDING_MIN=0.0005/OI_RETRACE=0.10)。待数据齐测 EF。

## permit 架构证伪 + EF 结果（14:37 完成）
- **EF 数据结果**：funding 正常(527币, 8h粒度)；**OI 全空**——Binance openInterestHist **仅保留最近30天**，窗口12-01~08-16超范围拉不到 → **F方案(OI背离)数据源硬限制, 无法回测**。
- **scheme4 funding(≥0.05%拥挤才开)**：169笔/48.5%/+281.5%/DD 355.7pp——**负优化**（funding极端正=多头仍在猛拉, 不是好入场时机; 等待条件期间错过大量信号）。
- **permit 架构最终结论（全方案对比, 旧逻辑=399笔/+1796.0%/DD220.7）**：
  | permit方案 | 笔数 | 毛cumPnl | maxDD |
  |---|---|---|---|
  | 立即 | 834 | +1589.9% | 519.4pp |
  | 延迟30m/1h | 592/584 | +1128/-1023% | 535/560pp |
  | 回撤10% | 297 | +209.3% | 528.3pp |
  | funding | 169 | +281.5% | 355.7pp |
- **permit 架构整体证伪**：所有入场点方案(0/1/2/4/6)全面劣于旧逻辑。框架性问题：①回撤爆炸(持仓堆积118+禁仓延迟) ②收益稀释(3次机会重复下注, 均盈亏1.5% vs 3.7%) ③条件等待错过妖币最佳入场点。
- **结论：维持旧架构(信号=立即开仓)定稿不变**；permit 代码保留(PERMIT=0默认关, ENTER_SCHEME/FUNDING_MIN/OI_RETRACE 参数已实现)。EF funding 数据(ef_*.csv 527币)保留备查, OI 数据不可得。fetch_ef_data.py 保留(未来30天内窗口可复用)。

## permit+age30m 方案交付（15:01 用户要求）
- 用户指定最新入场架构：**permit 且无持仓状态下, 距24h最高时间点>30m 才开仓；24h最高点止损(entry_h24锚)；单permit最多止损3次**。
- 实现：permit 加 n_stop 计数（stop_loss 平仓 +1, ≥3 封顶停开）；permit 开仓循环加 MIN_NEW_HIGH_AGE>30m 条件（复用参数）；CSV 新增 **permitOpenTime 列**（permit 开启时间, 649笔全非空）。
- **结果**：`_exp_permit_age30.csv` = **649笔/加权778/胜率53.5%/毛+793.5%/maxDD 629.2pp**（reason: expire331/pump224/expire_forced89/stop_loss4/take_profit1）。曲线 `_curve_permit_age30.html`。
- **对比旧逻辑(399笔/+1796.0%/DD220.7)：收益-1002pp、回撤+408pp——permit 框架问题依旧(持仓堆积+禁仓延迟+重复开仓稀释), age30m 入场未挽救。结论不变: 旧架构定稿最优。**

## permit bug 修复 + avaaiusdt 复查（15:33-15:53 用户严查）
- **Bug1(permit超时开仓)**：到期判断 `tick > end_ms` 严格大于 → `tick == end_ms` 时仍可开仓(cowusdt 08-16 19:00 超时第二单)。修复: `>=` 即失效。cowusdt 超时单消失。
- **Bug2(止损未先到先得)**：pump_stop 是独立 if 优先于 entry_h24/STOP_FACTOR——重构为三线取**最低触发价**(先到先得), reason 按触发价归属标注。
- **Bug3(信号被30m过滤拦截, avaaiusdt 18:20→19:10)**：MIN_NEW_HIGH_AGE/STAGNATE/PULLBACK 过滤错误残留在**信号检查**(旧架构实验遗留)——PERMIT 模式下信号只建 permit, 入场条件由 permit 开仓循环/entry_point_ok 判断。已加 `if not PERMIT:` 隔离。permitOpenTime 恢复正常(信号时刻 18:20)。
- **Bug4(单permit 4单超限)**：实现成"止损3次封顶"(开仓不限) 而非"最多3单"——改回 `n_open >= 3` 封顶。全样本分布 1单×125/2单×87/3单×191, 无超限。avaaiusdt 08-13 permit=3单(4→3)。
- **修复后结果**：`_exp_permit_age30.csv` = **872笔/加权1042/胜率28.0%/毛+1353.6%/maxDD 278.5pp**（vs 修复前 649笔/+793.5%/629.2pp: 收益+560pp、回撤-351pp）。曲线 `_curve_permit_age30.html` 已更新。
- **遗留设计问题**：entry_h24 贴脸止损(开仓≈24h高点 → 止损线高开仓价2-6% → 61%单小亏止损, 胜率28%)——建议加缓冲(×1.05) 或改滚动语义, 待用户拍板。

## permit 窗口结束规则叠加（17:01 用户拍板）
- **三条结束规则**：①距起始24h且无未平仓→立即结束 ②期内累计盈亏>0→立即结束(盈利即收手) ③已平仓3笔→立即结束。
- 实现：permit 循环到期改 `tick>=end_ms 且 sym not in holdings` 才结算(有持仓则保留等平, 不再开新仓)；平仓累计后 `total_pnl>0 或 n_open>=3` → 结算结束。**禁仓语义确认**：亏损禁7天在窗口结束后按总盈亏判断(总亏禁/总盈免), 已实现。
- **结果**：`_exp_permit_age30.csv` = **864笔/加权1034/胜率28.4%/毛+1528.5%/maxDD 246.8pp**（vs 结束规则前 872笔/+1353.6%/278.5pp: 收益+175pp、回撤-32pp）。permit 401个: 1单×126/2单×87/3单×188；permit 总盈亏正228/负173。曲线已更新。
- **距旧逻辑(399笔/+1796.0%/DD220.7)仍差: 收益-267pp、回撤+26pp**——entry_h24 贴脸止损(胜率28.4%)是主要拖累, 待用户定缓冲方案。

## 参数化+恢复入场+expire/pump结束规则（17:52-18:13 用户指令）
- **①MAX_PERMIT_OPENS 参数化**（默认3，兼作平仓结束阈值）；**②"重新设计(立即开+取消24h锚)"取消执行**——恢复 entry_h24 锚 + age30m 等待（permit 开启后距24h最高点>30m 才开）；**③新增结束规则④：持仓 expire(24h到期) 或 pump_stop(8h后新高) → permit 窗口立即结束**（结算含该笔，按总盈亏禁仓）。
- **MAX_OPEN 梯度**（恢复配置+新结束规则）：2=626笔/+935.7%/328.7pp；**3=772笔/胜率29.4%/+1580.6%/maxDD 224.9pp（最优）**；4=850笔/+1507.9%/307.3pp。
- **permit 架构最佳成绩**：MAX_OPEN=3 = **772笔/+1580.6%/224.9pp**（回撤几乎追平旧逻辑 220.7pp；收益距旧逻辑 +1796.0% 差 215pp）。曲线 `_curve_permit_age30.html` 已指向 mo3。
- gap 过滤梯度(10-20/25/30/40)已测证伪（胜率+18pp但收益-400~660pp、回撤恶化——贴脸止损单盈亏比3.4:1是大肉来源），实验文件清理。

## permit 兼容性验证通过（19:36-19:50 用户要求）
- 配置：PERMIT=1 + MIN_NEW_HIGH_AGE=0(立即开) + MAX_PERMIT_OPENS=1 + ENTRY_H24=0(取消24h锚) → **399笔/478/56.4%/+1796.0% 与旧版定稿完全一致**。
- 逐笔对比：399/399 开仓点一致，**唯一差异 skyaiusdt 04-29 pump_stop 平仓差5分钟(09:45 vs 09:40)、pnl相同(-10.8516%)**——来自 Bug2 修复时 pump_stop 触发从 `high>h1_prev` 改 `>=trigger`(等于即触发)，旧定稿 CSV 是重构前产物。
- **结论：permit 是旧逻辑的超集（1单立即开无锚 = 旧逻辑特例），兼容性确认。** 网络曾断(19:36 代理+直连全 000)等恢复后跑。ENTRY_H24 参数已加(默认1启用锚)。文件 `_exp_permit_compat.csv`。

## permit 架构正式定稿（20:14-20:28 用户拍板：permit版本替换旧版本）
- **定稿内容**：backtest_short_top1.py 改造为 **permit 架构正式实现**，默认定稿参数 = 兼容配置（PERMIT=1 + MIN_NEW_HIGH_AGE=0立即开 + MAX_PERMIT_OPENS=1 + ENTRY_H24=0无锚）→ 结果与旧逻辑一致 **399笔/478/56.4%/+1796.0%/maxDD 220.7pp**。
- 移植内容：信号→permit(24h允许状态) + permit开仓循环(结束规则①24h无持仓③平满N笔 + age30m/ENTRY_H24可选) + 平仓permit结算(规则②盈利收手④expire/pump结束, 按总盈亏禁仓) + Bug2修复(止损三线先到先得)。
- **自审通过**：与实验脚本兼容版(_exp_permit_compat)逐笔完全一致(399/399)；reason=expire255/pump140/stop3/tp1；maxDD 220.7pp；pnl[-80,80]。
- 清理：9 个 permit 实验文件(mo2/3/4、gap20~40、age30、compat)已删；ef_*.csv funding 数据保留。曲线 `_curve_pump8h.html` 更新(+1796.0%)。
- **permit 架构正式替换旧逻辑成为定稿**：PERMIT 参数默认1、MAX_PERMIT_OPENS 默认1、ENTRY_H24 默认0、MIN_NEW_HIGH_AGE 默认0；实验配置(3次机会/age30m/24h锚)代码保留可随时参数切换。定稿脚本还保留旧逻辑路径(PERMIT=0)。
