- 重构 utility.py：把 BinanceRank 专用的 6 个模块函数（fetch_ticker/parse/get_1h_base/fill_1h/fetch_server_time/split_rank）连同代理与 URL 常量（_HOSTNAME/_USE_PROXY/_LOCAL_IP/PROXIES/_PROXY_CHAIN/UA/TICKER_URL/KLINE_URL/TIME_URL）全部搬进 BinanceRank 类，作为类属性 + 实例方法；fetch_rank 调用全部改为 self.。删除 119 行冗余模块级代码。py_compile 通过，__main__ 一键调用方式不变。顺清 if not items 尾随空格；963/974 行 \D 警告为 Chrome 类原有，未动。

- 币安 USDT 永续 24h 排行榜采集器落地并验证通过：用户更换代理出口后，fapi.binance.com/fapi/v1/ticker/24hr 恢复 200（730 合约/682 USDT 永续），沿用 BinanceRank 原 fapi 数据源，不换 vision 镜像。
- 单次同步采集 OK：24h 上涨199/下跌477，耗时22.7s（含1h基准计算）。落盘结构 data/binance/rank_rise|fall/24h/{date}/{hour}/{time}.csv + latest.csv，字段 symbol/change/price/volume + data_time/mean_rise/mean_fall 元行，与 utility.py on_rank 一致。
- 已后台启动 workbuddy/binance_rank_collector.py（每10分钟一次，RUN_DAYS=30 自动退出）持续积累；首份快照 17_03_49.csv，第二份 17_05_14.csv 已落盘。
- 重要限制：币安无「历史排行榜」官方接口、本地也无历史分钟K，故只能从启动时刻起向前每10分钟积累，连续跑满30天才得「过去一个月」序列；后台进程依赖本机/会话常驻，要跑满需常驻机器启动（nohup/计划任务）。

- 历史 24h 排行榜回溯方案（用户纠正后）：用 fapi/v1/klines 拉 [一个月前-24h, 现在] 全部 1m K，每 10 分钟点 T 算 (close[T]-close[T-1440min])/close[T-1440min]*100，反推整月排行榜序列（无需向前积累）。算法已用 API 独立重算对拍验证一致（fall 文件 -0.0035421% ≈ 重算 -0.003541%）。
- 交付 workbuddy/binance_rank_backfill.py：相位1下载(每币种1m K缓存 npz + done.json 断点续传)、相位2计算(每10分钟生成 rank_rise|fall/24h/{date}/{hour}/{time}.csv + latest.csv，目录结构同 utility.py on_rank；volume=24h quote成交= prefix[T]-prefix[T-1440])。
- 实测 fapi klines 走 192.168.31.140:10811 代理可用(200)，但代理并发突发会间歇性抖动掉几个币种(返回连接错误/超时)。已加防护：整段失败不写 done、不写缓存；新版 work() 再加整币种4次重试、单页5次重试、并发 8→6。
- 已后台启动全量回溯(Jj638A, 682 USDT永续/31天)，首份采样正常。运行中是旧版(8并发/3重试)，掉的符号靠「重跑续传(新版)」补齐。下载完自动进入计算相位写 CSV。完成后需再跑一次脚本做 retry pass 补齐瞬时失败币种并重算。
- 重要：Bash 工具默认 2 分钟超时，sleep>120s 会被杀(空输出+exit1)。监控长任务不要 sleep 超 2 分钟，改直接查状态或用 TaskOutput 阻塞等待。

- 币安 418 IP 封禁排查与绕开：第一趟 8 并发拉 682 币×3万请求触发 IP(20.89.238.66=代理192.168.31.140:10811出口) ban。ticker/24hr(单次682权重)返回 418 body={"code":-1003,"msg":"...banned until 1786449299428..."}+retry-after:71秒(北京19:54:59解)。实测 klines 与 exchangeInfo(权重1)未被 ban 仍 200——封禁只针对高权重 ticker 接口。已把 backfill 的 get_symbols() 从 ticker/24hr 改为 exchangeInfo 拿 USDT 永续列表，续传完全绕开封禁。另给 work() 加 npz 复用(已缓存有效币秒跳过)，被杀重跑只补缺失。nohup 重启 pid 1962，npz 复用生效(225秒跳过)，正补剩余457缺失，下载完自动进计算相位重算 CSV。

- 补充(币安 ban 性质)：币安 -1003 是**全局 IP ban**（实测 klines 也返回 418，非仅 ticker/24hr）；ban 时长随反复触发递增（首 71s → 次 747s → 探测时递减至 241s，正自动解除）。根因是 klines 高并发持续请求(8并发)打爆代理出口 IP，非 ticker 一次性调用。用户指出「ticker/exchangeInfo 只拉一次、klines 才持续」完全正确。
- 环境后台进程注意：普通 Bash 内  启动的 python 会被会话回收(pid 1962/2060 均死)；须用 run_in_background 工具级后台(守护 uDQj9U 状态 running)。Git Bash 的       PID    PPID    PGID     WINPID   TTY         UID    STIME COMMAND
     1938    1937    1937       6180  ?         197609 19:58:54 /c/Users/lo-e/anaconda3/python
     1211       1    1211       7768  ?         197609 16:39:28 /usr/bin/bash
     2075       1    2075      10304  ?         197609 20:39:58 /usr/bin/bash
     2060       1    2059      10516  ?         197609 20:35:29 /usr/bin/bash
     1913       1    1913      16476  ?         197609 19:45:57 /usr/bin/bash
     2062    2060    2059      22628  ?         197609 20:35:30 /c/Users/lo-e/anaconda3/python
     1964    1962    1961      16868  ?         197609 20:03:34 /c/Users/lo-e/anaconda3/python
     1937       1    1937      24016  ?         197609 19:58:54 /usr/bin/bash
     1212    1211    1211      16348  ?         197609 16:39:28 /c/Users/lo-e/anaconda3/python
     2068       1    2068       8956  ?         197609 20:37:51 /usr/bin/bash
     1962       1    1961      18400  ?         197609 20:03:33 /usr/bin/bash
     2078    2075    2075        728  ?         197609 20:39:59 /usr/bin/ps
     1262       1    1262      17460  ?         197609 17:05:11 /usr/bin/bash
     1263    1262    1262       5832  ?         197609 17:05:11 /c/Users/lo-e/anaconda3/python
     2069    2068    2068       5696  ?         197609 20:37:51 /c/Users/lo-e/anaconda3/python
     1914    1913    1913      24004  ?         197609 19:45:57 /c/Users/lo-e/anaconda3/python 看不到 Windows python 进程、不可信，用 TaskOutput 查存活。
- 治理：binance_rank_backfill.py 的 MAX_WORKERS 改读环境变量 BW(默认2)；wait_and_run.py 守护改为读 banned until 精确休眠 + ban 解后 BW=1 低速续传 + 补齐完成才退出；work() 加 npz 复用(已缓存有效币秒跳过)。

- 切换小时K回溯以避封禁：用户要求把分钟K临时改小时K(减请求量避ban)，并保留已下分钟K缓存供后续复用；若有旧task在跑先停再跑新的。
- 操作：(1) TaskStop 停掉 ban 守护 uDQj9U；(2) 用 Get-CimInstance 查到孤儿回溯进程 PID 22968(binance_rank_backfill.py，20:50仍在写 cache_klines)并杀掉；(3) mv cache_klines -> cache_klines_1m 保留(255 npz/done 250)；(4) 改 binance_rank_backfill.py：新增 INTERVAL 环境变量(默认1h)+INTERVAL_MIN映射，CACHE_DIR 改成 cache_klines_{INTERVAL} 按周期隔离，fetch_klines 用 INTERVAL，build_arrays 填充逻辑改为按周期覆盖 _STEP_MIN 个分钟槽(小时K整段填该小时收盘、累计量填后入账)，MAX_WORKERS 默认6。
- 验证：冒烟(2币/3天/1h)通过；单元测试 build_arrays 确认同小时60槽一致=True 且模块算 BTC @08-10 12:00 24h=0.340953% vs 独立API重算 0.340952%(误差0.0001bp)，算法正确。
- 全量启动(task nmkGuU)：复用 symbols.json 682 币+2个冒烟npz，窗口 07-11 21:00~08-11 21:04，下载每币仅1次请求(1h)，进度顺利。注意：一次后台命令因 rm -rf 触发 safe-delete 批量删除确认而整条中止，故本次直接运行不删旧 data/(旧 collector 奇时间戳文件残留无害，会被同路径10分钟网格覆盖)。

- 【旧任务作废】用户手动终止所有旧任务并清空 workbuddy，旧排行榜回溯整套翻篇。
- 【新任务】BTCUSDT 永续 5m OHLC -> MongoDB：库 Workbuddy_5Min_Db，collection BTCUSDT.BINANCE（命名规则 <SYMBOL>.<EXCHANGE>，含点合法）。脚本 workbuddy/binance_5m_to_mongo.py：fapi/v1/klines 直连可用(无需代理, ping 200)，默认拉最近1小时(WINDOW_HOURS 环境变量可覆盖) 5mK；openTime 唯一索引 + replace_one upsert 幂等。测试通过：首跑拉12根全插(13:25→14:20 UTC, close 64179→64029)，重跑新插0条、未收盘K自动更新(64029.3→64052.1)。字段：openTime/open/high/low/close/volume/closeTime/quoteVolume/trades/takerBuyBaseVolume/takerBuyQuoteVolume。本地 MongoDB 服务 mongod 运行中(27017)。
- 【时间字段 datetime 化】openTime/closeTime 由毫秒 int 改存 datetime(UTC, BSON ISODate, 毫秒精度不丢)：主脚本加 ms_to_dt() 写入前转换；存量 12 条用 workbuddy/migrate_time_to_dt.py 一次性迁移；重跑验证：存量幂等匹配、窗口滑动自然增量 1 条(14:25) 亦 datetime。⚠️ 坑：bash 双引号内 $set 会被变量展开导致 pymongo 报 "update only works with $ operators"，带 $ 操作符的脚本必须独立成文件跑。
- 【忽略进行中 5m 块】endTime 对齐到最后一个完整 5m K 的 closeTime: end_ms=(now_ms//300000)*300000-1，进行中块不入库；插入前防御性 delete_many(openTime >= 当前进行中块 openTime) 自愈清理。验证：拉 12 根完整块(13:25→14:20)、清理误存进行中块 1 条、末条 14:20 close 刷新为收盘值 63924.6(此前未收盘存过 64029.3)。
- 【本地时区(用户拍板: UTC存储+本地字段)】openTime/closeTime 存 UTC datetime 物理精确，新增 openTimeLocal/closeTimeLocal 北京时间字符串(固定 UTC+8, 中国无夏令时)。存量 12 条用 migrate_time_to_dt.py 幂等补齐。验证 openTime=2026-08-11 13:25:00 UTC ↔ openTimeLocal=2026-08-11 21:25:00。
- 【24h涨跌幅计算完成(高效版)】workbuddy/binance_24h_change.py：时间点=今天00:00→当前最近整10分(23:00)共139点；所需278根5m K(先查库已有7根→缺271根一次拉565根筛选落库278，仅拉一次)；结果139点全 ok=139,error=0。落库 Workbuddy_5Min_Db/BTCUSDT.BINANCE_24H_CHANGE(139条, pointTime唯一幂等)，样例 00:00 change=-1.439861%(closeT 64268.8 vs closeT24h 65207.7)。已导出 workbuddy/btcusdt_24h_change_20260811.csv。
- ⚠️网络坑(记牢)：系统代理 127.0.0.1:10809(system, WORKBUDDY_PROXY_SOURCE=system)，python requests 默认读 HTTPS_PROXY 走它即通；代理挂时 ProxyError 握手超时，禁代理直连也 ConnectTimeout(直连不通, 必须走10809)；旧代理 192.168.31.140:10811 已失效。之前误存的 137 条 error 结果已被本次幂等覆盖为 ok。用户 23:07 确认代理恢复后重跑即全绿。
- 【涨跌幅改为只落 CSV】结果不再写 MongoDB：输出 workbuddy/data/change_20260811_20260811/btcusdt.csv(140 行含表头, 列 pointTimeLocal/closeT/closeT24h/changePct/status/errorDetail, 00:00→23:10 全 ok)。库内结果集 BTCUSDT.BINANCE_24H_CHANGE 已 drop，Workbuddy_5Min_Db 只剩 K 线源 BTCUSDT.BINANCE(280 根)。脚本 binance_24h_change.py 已改为只算+写 CSV，时间点动态到当前最近整10分。
- 【data/binance 复活之谜(解决)】根因=两个 binance_rank_collector.py 残留进程(16:39 启动 PID16348 / 17:05 启动 PID5832)在用户清空 workbuddy 后仍于内存运行，每10分钟写排行榜快照重建 workbuddy/data/binance(最新 23:19:53)。已用 psutil 定位命令行 → Stop-Process 杀掉，确认无 python 进程残留；现有残留快照移隔离 workbuddy/data/_trash_binance_20260811。经验：删脚本文件不停进程，找残留进程用 python psutil 查 cmdline(PowerShell Get-CimInstance 在沙箱静默失败)。
- 【范围扩到 08-10 起】用户要 2026-08-10 到现在：285 个整10分点(08-10 00:00→08-11 23:20)，所需 429 根 5m K(08-08 23:55→08-11 23:15)，库内已有 280、缺 149 一次拉 857 筛选落库 429；全 ok=285。输出 workbuddy/data/change_20260810_20260811/btcusdt.csv(285 行)。脚本 p_start 改固定 08-10 00:00、OUT_CSV 自动按 p_start~p_end 日期命名。
- 【扩到 08-09 起 + 双币】BTCUSDT+ETHUSDT：429 个整10分点(08-09 00:00→08-11 23:20)，每币所需 573 根 K(08-07 23:55→08-11 23:15 北京，连续 1144 根 5m)。⚠️坑：区间 1144 根 > limit 1000 单次被截断(币安只返回前 1000 根, 尾部丢)——BTC 靠旧数据侥幸补全、ETH 缺 73 根；已修 fetch 为分页 cursor 循环，重跑 ETH 拉 1145 根全补。两币 429 点全 ok。输出 change_20260809_20260811/{btcusdt,ethusdt}.csv(各 429 行)。经验：klines 区间 >1000 根必须分页，不能假设一次拉完。
