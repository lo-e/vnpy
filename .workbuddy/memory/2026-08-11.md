- 重构 utility.py：把 BinanceRank 专用的 6 个模块函数（fetch_ticker/parse/get_1h_base/fill_1h/fetch_server_time/split_rank）连同代理与 URL 常量（_HOSTNAME/_USE_PROXY/_LOCAL_IP/PROXIES/_PROXY_CHAIN/UA/TICKER_URL/KLINE_URL/TIME_URL）全部搬进 BinanceRank 类，作为类属性 + 实例方法；fetch_rank 调用全部改为 self.。删除 119 行冗余模块级代码。py_compile 通过，__main__ 一键调用方式不变。顺清 if not items 尾随空格；963/974 行 \D 警告为 Chrome 类原有，未动。
