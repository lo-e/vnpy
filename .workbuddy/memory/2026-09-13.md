# 2026-09-13 工作日志

## meme_short 前向监控 + 稳健性打磨
- 网络现状: Binance API 全死 (直连 502 / 代理10809 握手超时 / 4715 拒连), 无法补数据。Mongo 在线(707集合)但最新K线也止于 2026-08-28。样本外缺口 15 天。
- 新建 `meme_short/forward_watch.py`:
  - `dashboard`: 把定稿 _bt_mp9_final.csv(1263笔) 按**月度**拆。结果极强: 9/9 月全正, 最佳月(2026-01 +165%)仅占总额17.5%, 剔除最佳月仍 +780%, 稳定比 均值/σ=2.14。早停鲁棒性: 任一切点(月末停手)均盈利。
  - `status`: 数据缺口天数 + 网络/Mongo 状态。
  - `refresh [--dry]`: 数据恢复后自动调定稿引擎重跑扩展窗口, 与基线 diff 判 EDGE HOLD/DEGRADED/BROKEN。
  - `paper`: 当前纸面信号(基于最新可得信号点, 标注非实时)。
- 结论: edge 为 **BROAD-BASED**, 非个别月份堆出; 当前定稿可信度高。真·未来有效性待数据恢复后 `refresh` 验证。
- 产物: `data/change_20251201_20260828/_monthly_stability.csv` + `_monthly_stability.html` (echarts 暗色, 月盈亏柱+累计线+缩放/十字光标)。
- 后续: 代理恢复后优先 `binance_24h_change*.py` + `binance_5m_to_mongo.py` 补 08-29 至今窗口, 再 `forward_watch.py refresh` 跑真样本外。

## 网络/壳环境关键坑 (扩展 09-12 时踩中, 已解决)
- 代理可用! fapi.binance.com(合约) 经 127.0.0.1:10809 实际可达(返回实时价)。之前误判"全死"是 urllib 没走代理 + 误读陈旧日志。
- 坑1: 破壳全局 HTTPS_PROXY/HTTP_PROXY=127.0.0.1:14068 是坏的(502)。requests 自动读它→全 502。解法: 启动器 launch_gen.py 在 python 内 os.environ 设 10809 再 runpy.run_path 跑脚本。用 anaconda python(C:\Users\lo-e\anaconda3\python.exe, 有 requests+pymongo), managed python 无 requests。
- 坑2: 破壳 cwd 是工作区根 C:\Users\lo-e\Quant\vnpy 不是 meme_short。相对路径 python launch_gen.py 报 can't open file。解法: launch_gen.py 内 os.chdir(自身目录) + 后台命令用绝对路径 + 日志重定向用绝对路径。
- 坑3: 后台 `> gen_log.txt` 相对路径落到壳 cwd, 与脚本 data/(meme_short) 不一致→读错日志。一律绝对路径重定向。
- 10809 偶发 502 抖动, 降并发 BW=2 更稳; 脚本有代理自愈(60s 重试)。
- 后台运行中: launch_gen.py 生成 data/change_20251201_20260912/ 全窗口信号(只补 08-29~09-12 尾段), 跑完接 top1_analysis.py 聚合 → backtest_short_top1.py 重跑扩展窗口。

## 日志目录规范 (超要求, 已纠正)
- **所有本任务运行日志必须进数据窗口目录 `data/change_20251201_20260912/`**, 顶层 meme_short/ 只留脚本+通用文件 (本就是项目铁律, 之前第一次失败那次日志乱飞到工作区根=违规)。
- 落实: 当前正在跑的顶层 `gen_log.txt` 任务完成后归档为 `data/change_20251201_20260912/gen_24h_change.log`; 后续 top1_analysis / backtest / forward_watch 日志一律 `> data/change_20251201_20260912/*.log`。
- launch_gen.py 当前任务已用绝对路径启动(顶层 gen_log.txt), 不再改它; 若未来重跑改用数据文件夹路径。

## gen 扩展 09-12 进度 + regime 修正 (续)
- gen(binance_24h_change_all.py, 后台76Vi0R) **实际仍在跑**, 日志持续涨已到 [700/721] (还差~21币, 多为8月底新股 BYD/DJT/IONQ/MARA/TEM 等)。TaskOutput 标记 failed 是框架误把 stderr(dirname/cd null directory)当失败, python 进程活着。用户确认"拉完BTC就完整"正确: upsert_needed 把补的5m全写 Mongo(Workbuddy_5Min_Db/{sym}.BINANCE), BTCUSDT 含在内。
- regime 修正(关键): 原 extend_regime.py 从 fapi 抓 BTC 日线**无效**(只到08-28, fapi 日线按UTC自然日未收盘不返回09-12; 且口径与 Mongo 北京时间日线不一致)。正确路线: gen 把 BTC 5m 写 Mongo → **重跑 market_regime.py**(从 Mongo 读, get_regime_map/fetch_daily_close 口径一致) → regime_btc.csv 覆盖到 09-12。extend_regime.py 废弃不调用。
- 已写 `run_expand.py` 统一调度(日志全进 data/change_20251201_20260912/): ① market_regime.py 重算 ② top1_analysis.py 聚合(SRC_DIR=新窗口) ③ backtest_short_top1_persym.py 三版同源: final=REGIME_FILTER=0 / bear=REGIME_FILTER=1+REGIME_ALLOW=BEAR / nosideways=REGIME_FILTER=1+REGIME_ALLOW=BULL,BEAR; 三版各独立 BT_OUT + BT_PROGRESS(默认 progress 同目录 _bt_progress.json 会互盖, 必须独立)。gen 完成后一键 `python run_expand.py`。

## 日志命名规约 (超 2026-09-13 拍板, 固化)
- **所有日志文件名一律带 "_" 前缀**。例: `_run_expand.log` / `_step1_regime.log` / `_step3_bt_final.log` / `_step4_oos_final.log`。
- 已落实: ① `run_expand.py` 内所有 log_name 改为 `_` 前缀 + docstring 记录约定; ② 窗口目录已有日志全部改名加前缀(含运行中被占用的用 os.rename, 少数锁住的等跑完补)。
- 与既有产物命名一致: `_bt_mp9_final.csv` / `_bt_prog_final.json` / `_monthly_stability.html` 本就带 `_`, 日志对齐这条。

## regime 尾日守卫 (超要求: close 必须走完)
- `market_regime.py` 新增**剔除未走完的日**: 日线聚合加 `max_openTime`, 末尾不足整日的直接丢弃并打印。实测丢掉半截的 `2026-09-12`(只在 00:00~08:00 有K), regime_btc.csv 干净落在 **2026-09-11**(最后完整日, 覆盖 2560 天)。
- 原理: regime[D] 必须吃 D 日完整收盘(23:55 那根 close), 半截日会让 r20/r60/r120/r200 全偏; 08-27 的 r200=0.2462 距 BULL 阈值 0.25 只差 0.004, close 微动就翻类。
- 前视偏差: 无。引擎 `backtest_short_top1_persym.py:98-99` 用 `d1 = T日-1天` 查 regime_map, T 日信号用 T-1 日 regime(T-1 早已收盘)。

## 扩展到 09-12 的样本外结果 (first look)
- 全管线 `run_expand.py` 12:22 启动; bt_final 12:57 完成: **1322 笔 / 胜率 55.5% / 加权 943.09pp / maxDD 56.56 / ddRatio 0.0589**。基线(→08-28) 1263笔/945.66pp → 扩展两周 **-2.57pp 基本走平**。
- 按平仓时间切 08-29: 样本内 1256笔 +948.38pp(胜率56.37%) / **样本外 66笔 胜率39.39% -5.29pp**(13.5天)。
- 滚动分布对照: 样本外 14 天收益折 -5.50pp → 落在样本内 14 天滚动窗口的 **9.5 百分位**; 7天 20.9% / 21天 3.7%。样本内 14 天窗口 11.7% 为负(4320/36811), 最差 -49.66pp。→ **偏弱但在历史分布内, 非破位**。
- **核心诊断: 平仓原因结构反转**。样本内 expire=62%/pump_stop=38% → 样本外 expire=39%/**pump_stop=61%**。即"妖币轧空后 8h 缓冲外继续冲高"的频率大幅上升, 做空被反复打掉。
- **regime 首次真正换环境**: 样本内 271 天 97% BEAR(262天), 样本外 14 天 **11 天 BULL(79%)**+3天SIDEWAYS。BTC 08-20→09-11 +7.25%, 08-24/08-30 起多次判 BULL。
- 按 regime[T-1] 分组(用 permitOpenTime 锚, 引擎 gate 口径): 样本外 **BULL 44笔 +15.13pp(pump_stop 52.3%)** / **SIDEWAYS 22笔 -20.42pp(pump_stop 77.3%)**。样本内 SIDEWAYS 48笔 +66.04pp(好)、BULL 仅10笔 +17.06pp。→ OOS 亏损几乎全部来自 SIDEWAYS 桶, BULL 桶反而微赚; 但样本内 SIDEWAYS 是赚的, 故不宜就此判 nosideways 更优(小样本会翻脸)。
- 新建 `meme_short/oos_eval.py`: 传 `<bt_csv> [cutoff]`, 自动出 样本内/样本外分段 + 滚动同长度窗口百分位 + 月度分解 + regime[T-1] 分组 + 平仓原因对照。**踩坑修正: regime 锚点须用 permitOpenTime(信号 tick) 不是 openTime(成交 tick)**, 否则分组边界错位(44/22 vs 49/17)。
- 待办: bt_bear / bt_nosideways 跑完 → 三版 CSV+HTML 对比 + 判 EDGE HOLD/DEGRADED/BROKEN → 归档日志改名加 `_` 前缀。

## 三版定稿结果 (→2026-09-12, run_expand.py 全管线完成 14:33)
- **final(无regime, 定稿)**: 1322笔 / 胜率55.52% / 加权 **+943.09pp** / maxDD 56.56 / ddRatio 0.0589 / OOS(08-29→) 66笔 **-5.29pp**(14d百分位9.5%)。曲线 `_curve_3versions.html`。
- **nosideways(剔SIDEWAYS)**: 1270笔 / 54.72% / +865.79pp / ddRatio 0.0634 / OOS 61笔 **-15.47pp**(全BULL, 14d百分位5.5%)。
- **bear(只BEAR)**: 1198笔 / 55.59% / +865.29pp / ddRatio 0.0648 / **OOS 0笔**(14天里无BEAR日, 全被gate挡)。
- **判定: EDGE HOLD**(累计走平在分布内), 但带 regime 预警(见下)。
- **关键发现1 — edge 全在 BEAR**: 全窗口按 regime[T-1] 分组: BEAR 1198笔 +865.29pp / BULL 72笔 **+0.50pp(≈零)** / SIDEWAYS(样本内) 48笔 +66.04pp。策略历史 edge 实质=BEAR 环境专用。
- **关键发现2 — nosideways 的选择效应(反直觉)**: nosideways OOS(-15.47) 比 final(-5.29) **更差**。final 里 SIDEWAYS 交易占了 permit/持仓名额, 反而挤掉了部分 BULL 亏损单; 剔 SIDEWAYS 放进了 61 笔 BULL(pump_stop率63.9%) 全亏。"剔 SIDEWAYS"在 OOS 是负贡献, 与 2024 结论(SIDEWAYS 是毒药)相反 — 环境不同结论翻转, 印证不许凭小样本拍板。
- **关键发现3 — BULL 环境结构性偏弱**: OOS pump_stop 率 38%→64%(牛市轧空频繁), BULL 桶胜率 40.28%。BTC 08-24/08-30 起多次判 BULL(+7.25%)。**若 BULL 延续且 pump_stop 率维持高位, final 的 edge 可能被侵蚀 — 下次 refresh 重点盯 pump_stop 率与 BULL 桶盈亏**。
- 定稿维持 **REGIME_FILTER=0**(final 累计仍最高 943.09); bear 版虽躲过 OOS 回撤但样本内少赚 80pp, 不是免费保险。
- OOS 评估日志: `_step4_oos_final.log` / `_step4_oos_bear.log` / `_step4_oos_nosideways.log`。
- 日志 `_` 前缀全部落实(窗口目录内已无无前缀日志)。


## 收尾清理 (09-13 续, P1)
- 窗口 change_20251201_20260912 共741文件: 723原始信号csv + 18个_前缀产物。
- 归档中间产物到 _logs_archive/ (同盘mv可逆): 3个_bt_prog_*.json + 9个_*.log = 12文件。
- 保留核心: 三版_bt_mp9_*.csv + _curve_3versions.html + _done.json + regime_btc.csv。
- MEMORY 定稿参数T1加注: 裸跑默认08-16, 最新窗口经run_expand覆盖到09-12。
- 月稳定产物在旧窗口 change_20251201_20260828, 本次不动。
