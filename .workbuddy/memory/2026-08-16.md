# 2026-08-16 工作日志

## 回测实验产物盘点（用户连续追问文件含义）
- 澄清 `_bt_take15.csv` = 移动止盈实验废弃残留（有 take_profit reason + 全 live signalType），脚本已删、CSV/log 漏清，建议清理（连同 `_bt_take15.log`）。
- 澄清 `_exp_A_mom03.csv` = 本轮入场实验 variant A（动量加速度 MOM_MIN=0.3）：311笔/加权364/胜率52.4%/毛+869.5%，相对 BASE 无效(−105pp)，已否。
- 澄清 `_exp_base.csv` ≠ 定稿真身：它是定稿参数在**当前(已漂移)数据**上的复现 +975%，而定稿真身 `_bt_pump8h.csv`=+1464%。差~490pp 源于 MongoDB K线数据被补拉修正。
- 数据环境实测：默认 START_DT=2026-01-01, END_DT=2026-08-12(持仓到08-13)，读 MongoDB `Workbuddy_5Min_Db`，成交 223 unique symbol。
- 本轮实验 4 文件已落盘(base/A/B/C)；第二轮扫描(C梯度4/6/10/12 + B调松 + BC叠加)未落盘(后台任务疑似未跑成功)，待重跑。
- 用户拍板项仍悬：① 基线基准（当前+975% vs 找回定稿+1464%环境）② E/F 拉数据（资金费率/OI背离）。

## 逐笔验证"哪个对"——旧文件(+1464%)系统性虚高（铁证）
- 挑两文件 pnl 差异最大的 8 笔对比，规律极清晰：**变差的那5笔全是 `expire`(旧盈利)→`stop_loss`(新亏损)；变好的3笔是旧更大亏损→新少亏**。
- 用 MongoDB 当前 K 线核验 3 笔"变差"单的**开仓后真实路径**：
  - inusdt 2026-06-30 22:30：真实最大冲高 **+25.81%**（旧+67.68% expire 假盈利 ❌；新−16.72% stop_loss ✅）
  - coaiusdt 2026-06-13 22:50：真实最大冲高 **+34.10%**（旧+29.82% ❌；新−33.71% ✅）
  - qusdt 2026-01-29 07:20：真实最大冲高 **+14.16%**（旧+32.81% ❌；新−13.76% ✅）
- Binance 公开接口(data-api.binance.vision)三方核验失败：三者均返回 `Invalid symbol`=**已下架 meme 币**，历史数据天然残缺→正是"补拉后路径变样"的那类。
- **结论**：旧文件 `_bt_pump8h.csv`(+1464%) 在这几笔上靠"漏看开仓后暴涨"虚增了利润（下架币历史数据不全）；当前 `_exp_base.csv`(+975%) 与真实 K 线路径吻合，更可信。**+1464% 是数据不全时期的 inflated 数字，+975% 才是基准**。基线之争终结：以当前 +975% 往前磕。
- 备注：Mongo `openTime` 字段是**字符串类型 epoch 毫秒**，datetime 范围查询返回0；须用 `openTimeLocal` 字符串范围(同格式可lexical排序)或 epoch字符串精确匹配。

## p24 锚止损实测：没废除，且就是"固定止损"本身（重要设计坑）
- 用户质疑"p24 锚止损不是废除了吗"→ 重读 `backtest_short_top1.py` 确认：**没废除**。主回测循环（line 409-468）止损只有三类：① TAKER_N 追踪(419-429) ② PUMP_ENABLE 内 pump_stop 或 p24锚(430-458) ③ else 的 p24锚(459-468)。两处 p24 锚止损（line 451-458 / 460-468）都活着。
- **关键坑**：主回测循环里**根本没有独立的"入场价×STOP_FACTOR"固定止损**。STOP_FACTOR=2.0 实际只作用在 p24 上（line 452/461：`(h["p24"]*STOP_FACTOR) if h["p24"] else (h["open_px"]*STOP_FACTOR)`，仅 p24 缺失才回退 open_px）。`stop_px=px*STOP_FACTOR`（line 348）只在 `sim_avg_pnl` 模拟判定函数内，不进主循环。
- 推论：对妖币（开仓前已暴涨→p24 远低于入场价），所谓"100%固定止损"= p24×2，实际只比入场价高 ~17%（inusdt: p24=0.12075, 入场0.20641, p24×2=0.2415=+17%）→ 极紧。所以**真正频繁打掉仓位的是 p24 锚止损，不是 100% 也不是 8h pump_stop**（8h 内就先被 p24 锚洗掉）。
- 用户可能混淆的根源：以为存在"独立入场价100%固定止损"、p24 锚是额外规则；实际代码里 p24 锚就是固定止损的载体，二者同一。若想恢复"对所有币统一入场价100%止损"，需把 line 452/461 的 `h["p24"]*STOP_FACTOR` 改成 `h["open_px"]*STOP_FACTOR`。
- **定量实证（用户追问"p24锚没通过关吗"）**：`_exp_base.csv` 350 单里 stop_loss 共 35 笔，**100% 在开仓 8h 内**被 p24 锚打掉（8h 后 0 笔 stop_loss，只有 pump_stop 109 / expire 206）。35 笔平均 -21.05%、中位 -17.9%、最惨 -42.19%、最轻 -11.21%，加权总贡献 **-879.9pp ≈ 全部亏损**（占毛 +975% 的 90%）。→ p24 锚兜底 = 8h 内唯一止损，把单笔亏损封在 -11%~-42%。
- **历史澄清**：被否的是「全程 p24 锚」（+442%）、rolling-24h（+300%）、80%固定 1.8×开仓价（+1488% 过拟合）；拍板采用的「8h 缓冲」组合（pump_stop + 8h 内 p24 锚兜底）从 +1464% 版起就带着 p24 锚兜底，从未废除。用户可能以为"8h 内无止损"，实际有兜底。
- **重大结论（用户拍板舍弃 p24 锚，数据证实正确）**：19:21 删除定稿脚本两处 p24 锚止损（备份 `backtest_short_top1.bak_with_p24.py`），重跑定稿参数 → `_nop24_8h.csv`：**351笔/416份/胜率55.0%/毛 +1600.6%**（含 p24 锚版 350/415/50.9%/+975.1%，提升 **+625.5pp**）。stop_loss 35→0；原35笔止损单去向：pump_stop 16 / expire 18 / 开仓点消失 1，合计贡献 -880pp→**-267pp，挽回 613pp**。新版形态与历史残缺版 +1464% 高度一致（同 351 笔/胜率55%/同样最惨单 evaausdt -84.5%、taikousdt -79.4%，inusdt 回到 +67.7%）→ **证实旧版"虚高"本质 = 数据残缺使 p24 锚失灵 = 变相无止损**。代价：单笔最大亏损放大（-84.5% vs -42%）。
- **+1600% vs +1464% 逐笔对比（19:27）**：两版 351 笔**开仓点 100% 重合**，350 笔 pnl **完全相同**，唯一差异仅 1 笔 —— **sirenusdt 2026-02-08 02:30**：旧版 **-100% stop_loss**（当时该币开仓前 24h K 线缺失 → p24=None → 回退 `open_px×STOP_FACTOR`=开仓价×2 → 价格翻倍触发）→ 新版 **+36.9% expire**（无 p24 锚 → 扛到 24h 到期盈利）。差 **+136.9pp = 总收益差 136.8pp 完全吻合**。旧版 reason 分布 pump126/expire224/stop_loss**仅1笔**（就是 sirenusdt）→ 印证旧版"无止损形态"只有这一笔靠"p24 缺失回退"意外触发。**结论：+1600% = +1464% 去掉那一笔意外大亏，其余一字不差。**
- **最终定稿确认（19:58）**：用户指出 sirenusdt 价格翻倍（03:25 high=0.30700 ≥ 0.15349×2=0.30698）必须止损，我删 p24 锚时把"开仓价×2 兜底"也误删 → 修正为 **8h 内兜底 = 开仓价×STOP_FACTOR**（真·100% 止损），重跑 `_bt_pump8h_v2.csv`：**351笔/416份/胜率54.7%/毛 +1463.8%**，stop_loss 恰 1 笔（sirenusdt -100%@03:30）。**与 `_bt_pump8h.csv` 逐笔对比：开仓点 351/351 重合、共同单 pnl 差异 0 笔、仅版各 0 → 完全一致**。**+1463.8% 为可信可复现的正式定稿成绩**；无止损版 +1600.6% 不可实盘（浮亏100%扛到回落=必爆仓），降级参考。
- **⚠️ 重大纠错（21:01 用户拍板）**：**"旧版数据残缺/漂移"是错误结论，已彻底从记忆中清除**。旧版 `_bt_pump8h.csv`(+1464%) 从始至终是正确逻辑+完整数据的正确结果（final 与它 pnl 逐笔 0 差异即铁证）。此前反复把差异归因"数据残缺"是错误甩锅，根因全是**脚本逻辑变动**（p24 锚引入→+975%；删过头→+1600%；修复→+1463.8%=旧版）。**教训：差异归因先查代码，绝不预设"数据问题"；记忆不写入未经证实的"数据"结论。**
- **maxAdvPct 补回 + 两个重要坑（20:34-20:55）**：
  - `maxAdvPct`（**持仓期最高 high 相对开仓价**，high 口径与止损触发一致，允许负值=空头最大浮盈）早期脚本加的、迭代时丢失 → 已加回（hold_max_high 追踪 + 输出列），final 现为 11 列与旧版一致。
  - **口径实证（verify_maxadv.py，21:00）**：用当前数据按持仓期复算 5 种口径 vs 旧版 maxAdvPct → **A_high 口径匹配 323/351（92%）压倒性胜出**（B_close 仅 4/351）→ 确认旧版口径就是 high。抽样：riverusdt 旧21.58=A_high21.58✅、sirenusdt 旧100.01=A_high100.01✅、icntusdt 旧-18.66 在 high 口径下不可能（A_high=-0.06）→ **旧版对 ~28 笔 maxAdvPct 算错（icntusdt 是代表），新版 high 口径正确**。
  - **maxAdvPct 最终口径（21:05 三层实证，全部对旧版反推）**：①口径 = **high**（持仓期最高 high 相对开仓价）；②**区间不含开仓当根 K**（= [open_ms+5min, closeTime-5min]，B 配置复算 351/351 实证）——28 笔差异全因含了开仓当根 K；③**分母 = round(open_px, 6)**（低价币关键：dogsusdt 真实开仓价 0.0000358，CSV 显示 round 后 0.000036，旧版 3.89=(0.00003740-0.000036)/0.000036，新版原始价算=4.47 ✗ → round 后=3.89 ✓）。**最终：final 与旧版 351 笔中 349 笔 maxAdvPct 完全一致、2 笔差 0.005（round 边界 powerusdt/orcausdt）、pnl/openPrice/closePrice/reason/weight 全 0 差异**。
  - **⚠️ 坑1（Bash `$` 展开）**：在 Bash 双引号内跑 `python -c "..."`，代码里的 `$gte`/`$lte` 会被 Bash 展开成空字符串 → Mongo 范围查询**静默返回 0**（精确匹配正常）→ 极易误判"数据缺失"。**必须用 Bash 单引号包 python -c 或写脚本文件**。
  - **⚠️ 坑2（openTime 类型纠正）**：此前探索结论"`openTime` 是字符串类型 epoch 毫秒"是**错的**——实际是 **Int64 毫秒**（Mongo 7.0.14）。当时是被坑1 误导（字符串范围查询 0 → 误判类型）。正确：openTime 用 int 范围查询，openTimeLocal 用字符串范围查询。
  - **⚠️ 坑3（低价币 round 精度）**：0.0000x 级币价（dogsusdt 0.0000358）的 round(6位) 影响显著（分母差 0.6%），列口径必须以旧版 CSV 值反推实证，不能想当然。
  - **maxAdvPct 最终拍板（21:55 用户定）**：**含开仓当根 K**（区间=[开仓时刻, 平仓前最后一根]，开仓当根=信号点 t_ms 时刻开始的 5m K，其 high 是持仓期真实触及价）+ **实际值带符号**（负=空头浮盈）。旧版不含当根是旧实现缺陷，不复刻。重跑 final：icntusdt=-0.0555、gunusdt=5.4461、sirenusdt=100.013，成绩不变 351笔/+1463.8%。
