# 2026-08-30 工作日志

## meme_long：做多 24h 跌幅 Top1 镜像策略（复刻 meme_short）

### 边界（超 09:18 拍板）
- 工作目录锁定 `vnpy/workbuddy/meme_long`，生成文件绝不越界；vnpy 全量可读。
- 任务：把 `meme_short/backtest_short_top1.py`（做空 24h 涨幅 Top1 均值回归）镜像成**做多版**，信号改「24h 跌幅 Top1」。

### 已交付（均在 meme_long 内）
- `backtest_long_top1.py`：方向镜像引擎。PnL=(exit-open)/open×100（价涨盈）、止损价=open×(2-STOP_FACTOR)=open×0.2（跌80%止损）、止盈=open×1.8（涨80%）、开仓确认=24h最低≤30d最低（创30d新低）、pump_stop→dump_stop（滚动24h最低low队列）、CVD取反。permit 架构/加仓/禁仓逻辑原样复用。
- `market_regime.py`、`plot_final_curve.py`：方向无关，直接复制（曲线默认路径已改指 long 结果）。
- 信号：`data/change_20251201_20260828/000000_top1_fall.csv`（meme_short 现成产出，跌幅榜 Top1）。

### 关键修复（反前视/防挂死）
- 原 `ensure_close_map` 无条件 `probe_listing` 打网络；代理不可达时 `while not check_proxy(): sleep(60)` **无限死等** → 回测卡 38 分钟。
- 修复：①本地 Mongo 已覆盖全程则直接返回、跳过一切网络；②代理等待加 `PROXY_WAIT_CAP`（默认600s，本跑设0）超时放弃而非死等。

### 回测结果（窗口 2025-12-01~2026-08-28，离线 125 币）
- 219 笔 / 加权本金 32 / 胜率 35.6% / 加权平均盈亏 **−1.312%**（累计 −42.57%）。
- 平均盈 +17.07% / 平均亏 −10.89%。
- 方向验证：atuusdt 价跌→pnl−7.98%（long亏✓）；aprusdt 价涨→pnl+23.27%（long盈✓）。PnL 符号与做多语义一致。
- **结论：镜像后做多跌幅 Top1 是亏损的**，与 short 版（+945%/1263笔/56%胜率）形成真实不对称——暴跌妖币倾向继续跌/阴跌，均值回归做多不成立。非 bug，是数据属性。

### 已知限制
- 代理 `127.0.0.1:4715` 当前不可达 Binance，7 个币（akeusdt 等）本地 Mongo 缺数据无法补拉→跳过。完整全量回测需代理恢复后跑完整 `000000_top1_fall.csv`（311 币）。
- 309 行日志 `_bt_long_run.log` 可查逐笔进度。

### 关键诊断：long 只成交 47/132 币 ≠ 引擎丢信号（已证伪）
- 疑点：同窗口 short 1263 笔/313 币全参与，long 仅 219 笔/47 币(38%)。担心引擎静默丢信号。
- `diag_filters.py` 逐币逐信号重放静态过滤链：**83/132 币 confirm_new_low 100% 不通过**（24h 跌幅第一但信号时刻未创 30 天新低）→ 引擎正确拒绝。83 个 0 成交全部由静态过滤解释；仅 2 个"过滤放过却未成交"= akeusdt 等 7 个代理挂死缺数据币（诊断用残缺本地数据算伪通过），非逻辑 bug。
- 元因：RISE 侧 313 币 100% 过 confirm_new_high（暴涨币确创30d新高），FALL 侧仅 47 币创30d新低。
- **定论：镜像引擎逻辑忠实，负收益是真实结构不对称（暴跌币继续跌/阴跌，做多均值回归不成立；暴涨币倾向回落，做空才有效），非 bug。归档为负样本。**

### husdt 09:15 没开仓根因（忠实单币状态机复刻 diag_husdt_replay.py 验证）
- 复刻初版 bug：诊断脚本误把 `MIN_TOP1_PCT` 写成 +80（引擎是 -80），致所有信号被妖币过滤禁仓7天、0开仓；改回 -80 后复刻与真实引擎逐笔一致（04:40建permit/首开，06:45+06:50加仓，共3笔均expire）。**该 bug 仅存在于诊断脚本，引擎 backtest_long_top1.py 本身用 -80 正确，真实回测结果有效。**
- 09:15 真相：husdt 当时**已在 04:40 permit 持仓中**（permit 到 06-10 04:40），无法新开；只能走加仓。加仓三闸门：broke_flag=True（确实破过24h前低）✓、CVD背离=True ✓，但 **OFF_LOW_MAX=0.15 挡住**：09:15 价已较24h最低反弹 31.42%（>15%）→ 判定"追涨不做多"拒绝。09:10=24.4%、09:20=44.9% 同理被挡。
- 结论：09:15 没开仓是策略**正确行为**（OFF_LOW_MAX 防追高），非 bug；也再证 long 版"接下落刀"劣势（反弹太猛不愿追、不反弹就继续跌被 dump/expire）。
- 交付 `diag_husdt_replay.py`（忠实复刻单币 permit 状态机，可复用于未来任意币/时刻的开仓排查）。

## gen_cvd_kline.py 增加 CVD_MODE 开关（16:xx）
- 新增 `CVD_MODE` 环境变量，三态：
  - `both`(默认双状态, 橙▲value>0 + 蓝▼value<0 都标)
  - `short`：只标 value<0=卖盘增强/买盘衰竭(DIV)，即 **meme_short 做空加仓侧**(cvd_diverged 判定 rec<ref)
  - `long`：只标 value>0=卖盘衰减/吸筹(DIV)，即 **meme_long 做多加仓侧**(cvd_diverged_long 判定 rec>ref，backtest_long_top1.py:186-204；delta=2*tbqv−qv 口径与short完全一致，仅不等号相反)
- 文件名后缀：`_short`/`_long`/无(both)；SHOW_UP/SHOW_DOWN 两布尔经 .format 注入 JS 控 up/down scatter 渲染；图例/标题按模式改文案。
- 跑 HUSDT CVD_CENTER="2026-06-09 04:40:00" CVD_HALF_H=12 → data/cvd_husdt_20260609_0440_short.html(short,158蓝▼) / _long.html(long,143橙▲)；双状态版 cvd_husdt_20260609_0440.html 亦在。HUSDT库123629根(2025-06-25~2026-08-28 23:50)。
- 注：超原话"卖盘背离"先画了 short(value<0)，后澄清指 meme_long 加仓的"cvd卖盘衰减"=value>0(long)。两版都保留。
